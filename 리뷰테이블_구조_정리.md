# `reviews` 테이블 완전 가이드 ⭐

## 📌 테이블 목적

**파트너(병원/펫시터)에 대한 사용자 평가 및 리뷰 관리**

예약을 완료한 고객이 서비스를 받은 후 파트너에게 별점과 리뷰를 남길 수 있습니다.

---

## 📊 테이블 구조

### 전체 컬럼 목록

| 컬럼명 | 타입 | 필수 | 설명 |
|--------|------|------|------|
| **id** | BIGINT | ✅ | 리뷰 고유 번호 (자동 증가) |
| **user_id** | BIGINT | ✅ | 리뷰 작성자 ID (users 테이블) |
| **partner_id** | BIGINT | ✅ | 리뷰 대상 파트너 ID (partner 테이블) |
| **reservation_id** | BIGINT | ❌ | 연결된 예약 ID (선택적) |
| **rating** | DOUBLE | ✅ | 평점 (1.0 ~ 5.0) |
| **content** | TEXT | ❌ | 리뷰 내용 (선택적) |
| **created_at** | DATETIME | ✅ | 리뷰 작성 일시 |
| **updated_at** | DATETIME | ❌ | 리뷰 수정 일시 |

---

## 🔍 컬럼 상세 설명

### 1️⃣ 기본 정보

**id** (Primary Key)
- 리뷰 고유 번호
- 자동 증가 (AUTO_INCREMENT)
- 각 리뷰마다 고유한 번호

**created_at** (필수)
- 리뷰를 작성한 날짜/시간
- 자동 저장
- 수정 불가 (updatable = false)

**updated_at** (선택적)
- 리뷰를 마지막으로 수정한 날짜/시간
- 처음 작성 시에는 NULL
- 수정할 때마다 자동 업데이트

---

### 2️⃣ 관계 정보

**user_id** (Foreign Key → users 테이블) - 필수
- 리뷰를 작성한 고객의 ID
- 예: user_id = 5 (고객 "홍길동")
- 한 고객이 여러 파트너에게 리뷰 작성 가능

**partner_id** (Foreign Key → partner 테이블) - 필수
- 리뷰를 받는 파트너(병원/펫시터) ID
- 예: partner_id = 3 (서울동물병원)
- 한 파트너가 여러 리뷰를 받을 수 있음

**reservation_id** (선택적)
- 어느 예약에 대한 리뷰인지 연결
- NULL 가능
- 예: reservation_id = 10 (2025-11-20 진료 예약)
- **용도**: 해당 예약이 이미 리뷰를 받았는지 확인

---

### 3️⃣ 리뷰 내용

**rating** (필수) ⭐
- 파트너에 대한 평점
- DOUBLE 타입
- **범위**: 1.0 ~ 5.0
- **예시:**
  - 5.0 ⭐⭐⭐⭐⭐ (매우 만족)
  - 4.5 ⭐⭐⭐⭐☆
  - 3.0 ⭐⭐⭐ (보통)
  - 2.0 ⭐⭐ (불만족)
  - 1.0 ⭐ (매우 불만족)

**content** (선택적)
- 리뷰 내용 (텍스트)
- TEXT 타입 (긴 내용 가능)
- NULL 가능 (별점만 남기고 내용은 안 쓸 수도 있음)
- **예시:**
  - "진료가 꼼꼼하고 친절하셨습니다!"
  - "우리 강아지를 잘 돌봐주셨어요"
  - "시설이 깨끗하고 좋습니다"

---

## 💡 실제 데이터 예시

### 예시 1: 별점과 리뷰 모두 작성

```sql
INSERT INTO reviews VALUES (
  1,                              -- id
  5,                              -- user_id (고객 홍길동)
  3,                              -- partner_id (서울동물병원)
  10,                             -- reservation_id (2025-11-20 예약)
  5.0,                            -- rating (만점)
  '진료가 꼼꼼하고 친절하셨습니다!', -- content
  '2025-11-25 15:00:00',         -- created_at
  NULL                            -- updated_at (수정 안함)
);
```

**화면에 보이는 모습:**
```
⭐⭐⭐⭐⭐ 5.0
홍길동 | 2025-11-25
진료가 꼼꼼하고 친절하셨습니다!
```

---

### 예시 2: 별점만 작성 (리뷰 내용 없음)

```sql
INSERT INTO reviews VALUES (
  2,                              -- id
  7,                              -- user_id (고객 김철수)
  3,                              -- partner_id (서울동물병원)
  15,                             -- reservation_id
  4.0,                            -- rating
  NULL,                           -- content (내용 없음)
  '2025-11-24 10:30:00',         -- created_at
  NULL                            -- updated_at
);
```

**화면에 보이는 모습:**
```
⭐⭐⭐⭐ 4.0
김철수 | 2025-11-24
```

---

### 예시 3: 리뷰 수정한 경우

```sql
-- 처음 작성
INSERT INTO reviews VALUES (
  3,
  5,
  3,
  20,
  3.0,
  '보통이었어요',
  '2025-11-20 14:00:00',
  NULL
);

-- 나중에 수정
UPDATE reviews
SET
  rating = 4.5,
  content = '다시 생각해보니 정말 좋았습니다!',
  updated_at = '2025-11-25 16:00:00'
WHERE id = 3;
```

**화면에 보이는 모습:**
```
⭐⭐⭐⭐☆ 4.5
홍길동 | 2025-11-20 (수정됨: 2025-11-25)
다시 생각해보니 정말 좋았습니다!
```

---

## 📊 평균 평점 계산

파트너의 평균 평점을 계산할 때:

```sql
-- 서울동물병원(partner_id=3)의 평균 평점
SELECT
  AVG(rating) as avg_rating,
  COUNT(*) as review_count
FROM reviews
WHERE partner_id = 3;

-- 결과 예시:
-- avg_rating: 4.5
-- review_count: 10
```

**화면에 보이는 모습:**
```
서울동물병원
⭐ 4.5 (리뷰 10개)
```

---

## 🔗 다른 테이블과의 관계

```
users (고객)
  ↓ (user_id)
reviews ← 리뷰 작성
  ↓ (partner_id)
partner (병원/펫시터)

reservation (예약)
  ↓ (reservation_id) - 선택적 연결
reviews
```

**관계 설명:**
- **고객 한 명** → **여러 리뷰** 작성 가능 (1:N)
- **파트너 한 곳** → **여러 리뷰** 받을 수 있음 (1:N)
- **예약 한 건** → **리뷰 한 개**만 작성 가능 (1:1)

---

## 📋 주요 비즈니스 로직

### 1. 리뷰 작성 제약 조건

**한 예약당 하나의 리뷰만:**
```sql
-- 이미 리뷰가 있는지 확인
SELECT COUNT(*) FROM reviews
WHERE reservation_id = 10;

-- 결과가 0이면 리뷰 작성 가능
-- 결과가 1이면 이미 리뷰 작성됨
```

**본인이 사용한 서비스만 리뷰 작성 가능:**
```sql
-- 예약 완료 확인 후 리뷰 작성 허용
SELECT * FROM reservation
WHERE id = 10
  AND user_id = 5
  AND visit_date_time < NOW();  -- 예약 날짜가 지났는지
```

---

### 2. 리뷰 목록 조회

**파트너별 리뷰 조회 (최신순):**
```sql
SELECT
  r.id,
  r.rating,
  r.content,
  r.created_at,
  r.updated_at,
  u.nickname as user_name
FROM reviews r
JOIN users u ON r.user_id = u.id
WHERE r.partner_id = 3
ORDER BY r.created_at DESC;
```

**사용자가 작성한 리뷰 조회:**
```sql
SELECT
  r.id,
  r.rating,
  r.content,
  r.created_at,
  p.name as partner_name,
  p.partner_type
FROM reviews r
JOIN partner p ON r.partner_id = p.id
WHERE r.user_id = 5
ORDER BY r.created_at DESC;
```

---

### 3. 평점 통계

**평점별 리뷰 개수:**
```sql
SELECT
  FLOOR(rating) as star,
  COUNT(*) as count
FROM reviews
WHERE partner_id = 3
GROUP BY FLOOR(rating)
ORDER BY star DESC;

-- 결과 예시:
-- 5점: 50개
-- 4점: 30개
-- 3점: 15개
-- 2점: 4개
-- 1점: 1개
```

---

## 📱 API 사용 예시

### 1. 리뷰 작성

```http
POST http://localhost:9075/api/v1/reviews
Content-Type: application/json

{
  "userId": 5,
  "partnerId": 3,
  "reservationId": 10,
  "rating": 5.0,
  "content": "진료가 꼼꼼하고 친절하셨습니다!"
}
```

### 2. 파트너의 모든 리뷰 조회

```http
GET http://localhost:9075/api/v1/reviews/partner/3
```

**응답 예시:**
```json
{
  "ok": true,
  "data": [
    {
      "id": 1,
      "userId": 5,
      "userName": "홍길동",
      "partnerId": 3,
      "partnerName": "서울동물병원",
      "reservationId": 10,
      "rating": 5.0,
      "content": "진료가 꼼꼼하고 친절하셨습니다!",
      "createdAt": "2025-11-25T15:00:00",
      "updatedAt": null
    },
    {
      "id": 2,
      "userId": 7,
      "userName": "김철수",
      "partnerId": 3,
      "partnerName": "서울동물병원",
      "reservationId": 15,
      "rating": 4.0,
      "content": null,
      "createdAt": "2025-11-24T10:30:00",
      "updatedAt": null
    }
  ]
}
```

### 3. 리뷰 수정

```http
PUT http://localhost:9075/api/v1/reviews/1
Content-Type: application/json

{
  "rating": 4.5,
  "content": "다시 생각해보니 정말 좋았습니다!"
}
```

### 4. 리뷰 삭제

```http
DELETE http://localhost:9075/api/v1/reviews/1
```

---

## 🎯 데이터베이스 스키마 (예상)

```sql
CREATE TABLE IF NOT EXISTS reviews (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '리뷰 ID',
    user_id BIGINT NOT NULL COMMENT '리뷰 작성자 ID (users 테이블 FK)',
    partner_id BIGINT NOT NULL COMMENT '리뷰 대상 파트너 ID (partner 테이블 FK)',
    reservation_id BIGINT NULL COMMENT '연결된 예약 ID (선택적)',
    rating DOUBLE NOT NULL COMMENT '평점 (1.0 ~ 5.0)',
    content TEXT NULL COMMENT '리뷰 내용 (선택적)',
    created_at DATETIME NOT NULL COMMENT '작성 일시',
    updated_at DATETIME NULL COMMENT '수정 일시',

    INDEX idx_user_id (user_id),
    INDEX idx_partner_id (partner_id),
    INDEX idx_reservation_id (reservation_id),
    INDEX idx_rating (rating),
    INDEX idx_created_at (created_at),

    -- 한 예약당 하나의 리뷰만 가능
    UNIQUE KEY uk_reservation_id (reservation_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='리뷰 테이블';
```

---

## ⭐ 평점 시스템 설명

### 평점 범위
- **최소**: 1.0 ⭐
- **최대**: 5.0 ⭐⭐⭐⭐⭐
- **단위**: 0.5 또는 1.0 단위 권장

### 평점 의미
| 평점 | 별 | 의미 |
|------|-----|------|
| 5.0 | ⭐⭐⭐⭐⭐ | 매우 만족 (최고!) |
| 4.5 | ⭐⭐⭐⭐☆ | 만족 (좋아요) |
| 4.0 | ⭐⭐⭐⭐ | 만족 |
| 3.5 | ⭐⭐⭐☆ | 보통 (괜찮아요) |
| 3.0 | ⭐⭐⭐ | 보통 |
| 2.5 | ⭐⭐☆ | 아쉬움 |
| 2.0 | ⭐⭐ | 불만족 |
| 1.5 | ⭐☆ | 매우 불만족 |
| 1.0 | ⭐ | 최악 |

---

## 📌 핵심 포인트

### ✅ 필수 입력 항목
1. **user_id** - 누가 작성했는지
2. **partner_id** - 어느 파트너에 대한 리뷰인지
3. **rating** - 평점 (1.0~5.0)

### ❌ 선택 입력 항목
1. **reservation_id** - 예약과 연결 (선택)
2. **content** - 리뷰 내용 (별점만도 가능)

### 🔄 자동 관리 항목
1. **id** - 자동 증가
2. **created_at** - 작성 시 자동 저장
3. **updated_at** - 수정 시 자동 업데이트

---

## 🎨 프론트엔드 UI 예시

### 리뷰 작성 화면
```
┌─────────────────────────────┐
│   리뷰 작성                  │
├─────────────────────────────┤
│ 서울동물병원                 │
│ 2025-11-20 진료 예약        │
├─────────────────────────────┤
│ 평점을 선택하세요            │
│ ☆ ☆ ☆ ☆ ☆                  │
│                             │
│ 리뷰 내용 (선택)            │
│ ┌─────────────────────────┐ │
│ │                         │ │
│ │                         │ │
│ └─────────────────────────┘ │
│                             │
│  [리뷰 작성하기]            │
└─────────────────────────────┘
```

### 리뷰 목록 화면
```
┌─────────────────────────────┐
│ 서울동물병원                 │
│ ⭐ 4.5 (리뷰 100개)          │
├─────────────────────────────┤
│ ⭐⭐⭐⭐⭐ 5.0               │
│ 홍길동 | 2025-11-25         │
│ 진료가 꼼꼼하고 친절하셨습니다!│
├─────────────────────────────┤
│ ⭐⭐⭐⭐ 4.0                │
│ 김철수 | 2025-11-24         │
├─────────────────────────────┤
│ ⭐⭐⭐⭐⭐ 5.0               │
│ 이영희 | 2025-11-23         │
│ 시설이 깨끗하고 좋습니다     │
└─────────────────────────────┘
```

---

## 💼 비즈니스 활용

### 1. 파트너 랭킹
평균 평점이 높은 파트너를 상위에 노출

### 2. 신뢰도 표시
리뷰 개수가 많을수록 신뢰도 향상

### 3. 서비스 개선
낮은 평점의 리뷰를 분석하여 서비스 개선

### 4. 마케팅
높은 평점의 리뷰를 홍보 자료로 활용
