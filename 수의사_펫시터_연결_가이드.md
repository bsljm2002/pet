# 수의사/펫시터 프론트엔드-백엔드 연결 가이드

## 📋 구현 완료 사항

### ✅ 백엔드 (Spring Boot)

#### 1. 도메인 모델
- **위치**: `backend/demo/src/main/java/com/example/pet/demo/partner/`
- **파일**:
  - `domain/PartnerType.java` - 파트너 유형 Enum (HOSPITAL/SITTER)
  - `domain/Partner.java` - 통합 엔티티 (수의사/펫시터)
  - `domain/PartnerRepository.java` - JPA Repository

#### 2. DTO
- **위치**: `backend/demo/src/main/java/com/example/pet/demo/partner/api/dto/`
- **파일**:
  - `PartnerListRes.java` - 목록 조회 응답 DTO
  - `PartnerCreateReq.java` - 생성 요청 DTO

#### 3. 서비스 레이어
- **위치**: `backend/demo/src/main/java/com/example/pet/demo/partner/app/`
- **파일**: `PartnerService.java`
- **기능**:
  - 파트너 목록 조회 (유형별, 전문분야별 필터링)
  - 파트너 상세 조회
  - 파트너 생성
  - 파트너 정보/위치/이미지 업데이트
  - 영업 상태 토글
  - 평점 업데이트

#### 4. API 컨트롤러
- **위치**: `backend/demo/src/main/java/com/example/pet/demo/partner/api/`
- **파일**: `PartnerController.java`
- **엔드포인트**:
  - `GET /api/v1/partners?type={HOSPITAL|SITTER}&specialty={전문분야}` - 목록 조회
  - `GET /api/v1/partners/{partnerId}` - 상세 조회
  - `POST /api/v1/partners` - 생성
  - `PUT /api/v1/partners/{partnerId}` - 정보 수정
  - `PATCH /api/v1/partners/{partnerId}/location` - 위치 정보 수정
  - `PATCH /api/v1/partners/{partnerId}/image` - 이미지 수정
  - `PATCH /api/v1/partners/{partnerId}/toggle-open` - 영업 상태 토글
  - `GET /api/v1/partners/my?userId={userId}` - 내 파트너 목록

### ✅ 프론트엔드 (Flutter)

#### 1. 서비스
- **위치**: `front/main/lib/services/`
- **파일**:
  - `vet_service.dart` - 수의사/펫시터 전용 서비스 (새로 생성)
  - `api_service.dart` - 기존 API 서비스 업데이트

#### 2. API 연결
- **baseUrl 변경**: `http://10.0.2.2:9075/api/v1`
- **수의사 목록**: `/partners?type=HOSPITAL`
- **펫시터 목록**: `/partners?type=SITTER`

#### 3. Provider 연결
- **파일**: `front/main/lib/providers/hospital_provider.dart`
- **상태**: 이미 구현되어 있음 (ApiService 사용)

## 🚀 사용 방법

### 백엔드 실행

```bash
cd backend/demo
./mvnw spring-boot:run
```

또는

```bash
cd backend/demo
./mvnw clean install
java -jar target/demo-0.0.1-SNAPSHOT.jar
```

### 프론트엔드 실행

```bash
cd front/main
flutter pub get
flutter run
```

### API 테스트

#### 1. 수의사 목록 조회
```bash
curl -X GET "http://localhost:9075/api/v1/partners?type=HOSPITAL"
```

#### 2. 펫시터 목록 조회
```bash
curl -X GET "http://localhost:9075/api/v1/partners?type=SITTER"
```

#### 3. 전문분야로 필터링
```bash
curl -X GET "http://localhost:9075/api/v1/partners?type=HOSPITAL&specialty=내과"
```

#### 4. 파트너 생성 (예시)
```bash
curl -X POST "http://localhost:9075/api/v1/partners" \
  -H "Content-Type: application/json" \
  -d '{
    "partnerType": "HOSPITAL",
    "name": "루나 동물의료센터",
    "doctorName": "김루나 대표원장",
    "address": "서울특별시 강남구 테헤란로 218",
    "latitude": 37.5012743,
    "longitude": 127.0396597,
    "phone": "02-501-1200",
    "description": "최고의 동물병원입니다.",
    "specialties": ["내과", "외과", "영상의학"],
    "availableTimes": ["09:00", "10:30", "14:00", "16:30"],
    "education": ["서울대학교 수의과대학 졸업", "대한수의학회 인증 수의사"]
  }'
```

## ⚠️ 부족한 부분 및 추가 작업 필요 사항

### 1. 데이터베이스 설정
**상태**: ❌ 미완료
**필요 작업**:
- `application.properties` 또는 `application.yml`에 데이터베이스 설정
- MySQL/PostgreSQL 등 실제 데이터베이스 연결
- 테이블 자동 생성을 위한 JPA 설정

**예시 설정** (`application.properties`):
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/petcare
spring.datasource.username=root
spring.datasource.password=your_password
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
```

### 2. 초기 데이터 삽입
**상태**: ❌ 미완료
**필요 작업**:
- 더미 데이터를 실제 데이터베이스에 삽입
- `CommandLineRunner` 또는 SQL 스크립트를 사용하여 초기 데이터 생성

**예시** (CommandLineRunner):
```java
@Component
public class DataInitializer implements CommandLineRunner {
    @Autowired
    private PartnerRepository partnerRepository;

    @Override
    public void run(String... args) {
        if (partnerRepository.count() == 0) {
            // 초기 데이터 삽입
            Partner partner = Partner.builder()
                .partnerType(PartnerType.HOSPITAL)
                .name("루나 동물의료센터")
                .address("서울특별시 강남구 테헤란로 218")
                // ... 기타 정보
                .build();
            partnerRepository.save(partner);
        }
    }
}
```

### 3. 사용자 인증/인가
**상태**: ❌ 미완료
**필요 작업**:
- Spring Security 설정
- JWT 또는 세션 기반 인증
- 파트너 등록/수정 시 권한 체크

### 4. 이미지 업로드 기능
**상태**: ⚠️ 부분 완료
**현재 상태**:
- 백엔드에 이미지 URL 업데이트 API는 존재
- 실제 이미지 업로드 엔드포인트는 미구현

**필요 작업**:
- 파트너 이미지 업로드 엔드포인트 추가
- FileStorageService 활용

**추가할 엔드포인트** (PartnerController.java):
```java
@PostMapping(value = "/{partnerId}/image", consumes = MediaType.MULTIPART_FORM_DATA_VALUE)
public ResponseEntity<ApiResponse<Map<String, String>>> uploadPartnerImage(
        @PathVariable Long partnerId,
        @RequestParam("file") MultipartFile file
) throws Exception {
    String relative = fileStorageService.savePartnerImage(partnerId, file);
    String imageUrl = ServletUriComponentsBuilder.fromCurrentContextPath()
            .path(relative)
            .toUriString();
    partnerService.updateImage(partnerId, imageUrl);
    return ResponseEntity.ok(ApiResponse.ok(Map.of("imageUrl", imageUrl)));
}
```

### 5. 위치 기반 거리 계산
**상태**: ❌ 미완료
**필요 작업**:
- 사용자의 현재 위치 가져오기
- Haversine 공식을 사용한 거리 계산
- 거리순 정렬 기능

**예시 (PartnerService.java에 추가)**:
```java
/**
 * 거리 계산 (Haversine 공식)
 */
private double calculateDistance(double lat1, double lon1, double lat2, double lon2) {
    final int R = 6371; // 지구 반지름 (km)

    double latDistance = Math.toRadians(lat2 - lat1);
    double lonDistance = Math.toRadians(lon2 - lon1);
    double a = Math.sin(latDistance / 2) * Math.sin(latDistance / 2)
            + Math.cos(Math.toRadians(lat1)) * Math.cos(Math.toRadians(lat2))
            * Math.sin(lonDistance / 2) * Math.sin(lonDistance / 2);
    double c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));

    return R * c;
}

/**
 * 위치 기반 파트너 목록 조회
 */
public List<PartnerListRes> getNearbyPartners(
    String partnerType,
    Double userLat,
    Double userLon,
    Double radiusKm
) {
    // ... 구현
}
```

### 6. 예약 시스템 연동
**상태**: ⚠️ 부분 완료
**현재 상태**:
- ReservationController는 존재
- Partner와의 연동 필요

**필요 작업**:
- 예약 생성 시 partnerId 추가
- 예약 가능 시간 체크 로직
- 예약 승인/거부 기능

### 7. 리뷰/평점 시스템
**상태**: ❌ 미완료
**필요 작업**:
- Review 엔티티 생성
- 리뷰 작성/조회/수정/삭제 API
- 평균 평점 자동 계산

**예시 구조**:
```java
@Entity
public class Review {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne
    private Partner partner;

    @ManyToOne
    private User user;

    private Double rating;
    private String content;
    private LocalDateTime createdAt;
}
```

### 8. 검색 기능 개선
**상태**: ⚠️ 부분 완료
**현재 상태**:
- 전문분야로 LIKE 검색 가능
- 이름으로 검색 가능

**필요 작업**:
- 복합 검색 (이름 + 전문분야 + 지역)
- 정렬 기능 (거리순, 평점순, 이름순)
- 페이징 처리

### 9. 캐싱
**상태**: ❌ 미완료
**필요 작업**:
- Redis 캐싱
- 파트너 목록 캐싱
- 캐시 무효화 전략

### 10. 에러 처리 개선
**상태**: ⚠️ 부분 완료
**필요 작업**:
- 커스텀 예외 클래스 생성
- GlobalExceptionHandler 추가
- 일관된 에러 응답 형식

### 11. 테스트 코드
**상태**: ❌ 미완료
**필요 작업**:
- Unit Test (Service 계층)
- Integration Test (Controller 계층)
- 프론트엔드 Widget Test

### 12. 카카오맵 API 키 설정
**상태**: ❌ 미완료
**필요 작업**:
- 카카오 개발자 센터에서 JavaScript 키 발급
- `front/main/lib/main.dart`의 API 키 교체
- 자세한 내용은 `카카오맵_설정_가이드.md` 참조

## 📊 우선순위

### 🔴 높음 (즉시 필요)
1. 데이터베이스 설정 및 연결
2. 초기 데이터 삽입
3. 카카오맵 API 키 설정

### 🟡 중간 (곧 필요)
4. 이미지 업로드 기능 완성
5. 위치 기반 거리 계산
6. 예약 시스템 연동
7. 에러 처리 개선

### 🟢 낮음 (추후 개선)
8. 리뷰/평점 시스템
9. 검색 기능 개선
10. 캐싱
11. 사용자 인증/인가
12. 테스트 코드

## 📝 다음 단계

1. **데이터베이스 설정**
   ```bash
   # MySQL 설치 및 데이터베이스 생성
   CREATE DATABASE petcare;
   ```

2. **application.properties 설정**
   ```properties
   spring.datasource.url=jdbc:mysql://localhost:3306/petcare
   spring.datasource.username=root
   spring.datasource.password=your_password
   spring.jpa.hibernate.ddl-auto=update
   ```

3. **백엔드 재시작 및 테이블 생성 확인**
   ```bash
   ./mvnw spring-boot:run
   ```

4. **초기 데이터 삽입** (SQL 또는 CommandLineRunner)

5. **프론트엔드에서 테스트**
   ```bash
   flutter run
   ```

6. **카카오맵 API 키 설정** (의사 상세보기 지도 기능용)

## 🔗 관련 파일

### 백엔드
- `backend/demo/src/main/java/com/example/pet/demo/partner/`
  - `domain/Partner.java`
  - `domain/PartnerRepository.java`
  - `app/PartnerService.java`
  - `api/PartnerController.java`
  - `api/dto/PartnerListRes.java`
  - `api/dto/PartnerCreateReq.java`

### 프론트엔드
- `front/main/lib/services/vet_service.dart`
- `front/main/lib/services/api_service.dart`
- `front/main/lib/providers/hospital_provider.dart`
- `front/main/lib/models/vet_model.dart`
- `front/main/lib/models/sitter_model.dart`

## 💡 참고사항

- 백엔드 API는 RESTful 설계 원칙을 따릅니다
- 프론트엔드는 Provider 패턴으로 상태 관리합니다
- 더미 데이터는 개발 중에만 사용되며, 프로덕션에서는 실제 데이터베이스를 사용해야 합니다
- 카카오맵 기능은 JavaScript 키가 필요합니다 (무료)
