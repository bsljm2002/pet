# 이메일 인증 & 간편 로그인 구현 가이드

## 📧 Part 1: 이메일 인증 기능

### 1-1. 백엔드 구현 (Spring Boot)

#### 📦 필요한 의존성 추가 (`build.gradle`)

```gradle
dependencies {
    // 이메일 발송
    implementation 'org.springframework.boot:spring-boot-starter-mail'

    // Redis (인증 코드 임시 저장용 - 선택사항)
    implementation 'org.springframework.boot:spring-boot-starter-data-redis'
}
```

#### ⚙️ 이메일 설정 (`application.yml`)

```yaml
spring:
  mail:
    host: smtp.gmail.com  # Gmail 사용 시
    port: 587
    username: your-email@gmail.com
    password: your-app-password  # Gmail 앱 비밀번호
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

    # 또는 Naver 사용 시
    # host: smtp.naver.com
    # port: 465
    # properties:
    #   mail:
    #     smtp:
    #       ssl:
    #         enable: true
```

#### 📧 이메일 서비스 생성

**`EmailService.java`**
```java
package com.example.pet.demo.email;

import org.springframework.mail.SimpleMailMessage;
import org.springframework.mail.javamail.JavaMailSender;
import org.springframework.stereotype.Service;
import lombok.RequiredArgsConstructor;
import java.util.Random;
import java.util.concurrent.ConcurrentHashMap;

@Service
@RequiredArgsConstructor
public class EmailService {

    private final JavaMailSender mailSender;

    // 인증 코드 임시 저장 (실제로는 Redis 사용 권장)
    private final ConcurrentHashMap<String, String> verificationCodes = new ConcurrentHashMap<>();

    /**
     * 6자리 인증 코드 생성
     */
    public String generateVerificationCode() {
        Random random = new Random();
        return String.format("%06d", random.nextInt(1000000));
    }

    /**
     * 이메일 인증 코드 발송
     */
    public void sendVerificationCode(String email) {
        String code = generateVerificationCode();

        // 인증 코드 저장 (5분 후 자동 삭제 - 실제로는 Redis TTL 사용)
        verificationCodes.put(email, code);

        SimpleMailMessage message = new SimpleMailMessage();
        message.setTo(email);
        message.setSubject("[피터펫] 이메일 인증 코드");
        message.setText(
            "안녕하세요, 피터펫입니다.\n\n" +
            "회원가입을 위한 인증 코드는 다음과 같습니다:\n\n" +
            code + "\n\n" +
            "이 코드는 5분간 유효합니다.\n" +
            "본인이 요청하지 않았다면 이 메일을 무시하세요."
        );

        mailSender.send(message);
    }

    /**
     * 인증 코드 확인
     */
    public boolean verifyCode(String email, String code) {
        String savedCode = verificationCodes.get(email);
        if (savedCode != null && savedCode.equals(code)) {
            verificationCodes.remove(email); // 사용 후 삭제
            return true;
        }
        return false;
    }
}
```

#### 🎯 컨트롤러에 API 추가

**`UserController.java`에 추가**
```java
@RestController
@RequestMapping("/api/v1/users")
@RequiredArgsConstructor
public class UserController {

    private final UserService userService;
    private final EmailService emailService;

    /**
     * 이메일 인증 코드 발송
     */
    @PostMapping("/send-verification-code")
    public ResponseEntity<ApiResponse<Void>> sendVerificationCode(
            @RequestParam String email) {
        try {
            emailService.sendVerificationCode(email);
            return ResponseEntity.ok(
                ApiResponse.ok(null, "인증 코드가 발송되었습니다.")
            );
        } catch (Exception e) {
            return ResponseEntity.badRequest().body(
                ApiResponse.error("이메일 발송에 실패했습니다: " + e.getMessage())
            );
        }
    }

    /**
     * 이메일 인증 코드 확인
     */
    @PostMapping("/verify-code")
    public ResponseEntity<ApiResponse<Map<String, Boolean>>> verifyCode(
            @RequestParam String email,
            @RequestParam String code) {
        boolean isValid = emailService.verifyCode(email, code);
        Map<String, Boolean> result = Map.of("verified", isValid);

        if (isValid) {
            return ResponseEntity.ok(
                ApiResponse.ok(result, "인증이 완료되었습니다.")
            );
        } else {
            return ResponseEntity.badRequest().body(
                ApiResponse.error("인증 코드가 일치하지 않습니다.")
            );
        }
    }
}
```

#### 📧 Gmail 앱 비밀번호 생성 방법

1. Google 계정 설정 → 보안
2. 2단계 인증 활성화
3. "앱 비밀번호" 생성
4. 생성된 16자리 비밀번호를 `application.yml`에 입력

---

### 1-2. 프론트엔드 구현 (Flutter)

#### 📱 이메일 인증 서비스 추가

**`lib/services/email_verification_service.dart`**
```dart
import 'dart:convert';
import 'package:http/http.dart' as http;

class EmailVerificationService {
  static const String baseUrl = "http://10.0.2.2:9075/api/v1/users";

  /// 이메일 인증 코드 발송
  Future<Map<String, dynamic>> sendVerificationCode(String email) async {
    final url = Uri.parse("$baseUrl/send-verification-code?email=$email");

    try {
      final response = await http.post(url);

      if (response.statusCode == 200) {
        final data = jsonDecode(response.body);
        return {
          "success": true,
          "message": data["message"] ?? "인증 코드가 발송되었습니다."
        };
      } else {
        return {
          "success": false,
          "message": "인증 코드 발송에 실패했습니다."
        };
      }
    } catch (e) {
      return {
        "success": false,
        "message": "네트워크 오류: $e"
      };
    }
  }

  /// 이메일 인증 코드 확인
  Future<Map<String, dynamic>> verifyCode(String email, String code) async {
    final url = Uri.parse("$baseUrl/verify-code?email=$email&code=$code");

    try {
      final response = await http.post(url);

      if (response.statusCode == 200) {
        final data = jsonDecode(response.body);
        return {
          "success": true,
          "verified": data["data"]["verified"] ?? false,
          "message": data["message"] ?? "인증이 완료되었습니다."
        };
      } else {
        final data = jsonDecode(response.body);
        return {
          "success": false,
          "verified": false,
          "message": data["message"] ?? "인증에 실패했습니다."
        };
      }
    } catch (e) {
      return {
        "success": false,
        "verified": false,
        "message": "네트워크 오류: $e"
      };
    }
  }
}
```

#### 📝 회원가입 화면에 인증 단계 추가

**`signup_screen.dart` 수정 예시**

```dart
// 이메일 입력 후 인증 코드 발송
void _sendVerificationCode() async {
  final email = '${_emailLocalController.text}@${_selectedEmailDomain}';

  final result = await EmailVerificationService().sendVerificationCode(email);

  if (result['success']) {
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(content: Text(result['message']), backgroundColor: Colors.green),
    );
    // 인증 코드 입력 단계로 이동
    _changeStep(nextStep);
  } else {
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(content: Text(result['message']), backgroundColor: Colors.red),
    );
  }
}

// 인증 코드 확인
void _verifyCode() async {
  final email = '${_emailLocalController.text}@${_selectedEmailDomain}';
  final code = _verificationCodeController.text;

  final result = await EmailVerificationService().verifyCode(email, code);

  if (result['verified']) {
    setState(() {
      _isEmailVerified = true;
    });
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(content: Text(result['message']), backgroundColor: Colors.green),
    );
    // 다음 단계로 이동
    _changeStep(nextStep);
  } else {
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(content: Text(result['message']), backgroundColor: Colors.red),
    );
  }
}
```

---

## 🔐 Part 2: 간편 로그인 (소셜 로그인)

### 2-1. Google 로그인

#### 📦 패키지 설치

```yaml
# pubspec.yaml
dependencies:
  google_sign_in: ^6.1.5
  firebase_auth: ^4.15.0  # Firebase 사용 시
```

#### ⚙️ Android 설정

**`android/app/build.gradle`**
```gradle
android {
    defaultConfig {
        // Google OAuth Client ID 추가
    }
}
```

**Firebase Console 설정**
1. Firebase 프로젝트 생성
2. Android 앱 추가 (패키지명 입력)
3. `google-services.json` 다운로드 → `android/app/` 폴더에 복사
4. Authentication → Sign-in method → Google 활성화

#### 🔧 Google 로그인 서비스

**`lib/services/google_signin_service.dart`**
```dart
import 'package:google_sign_in/google_sign_in.dart';
import 'package:firebase_auth/firebase_auth.dart';

class GoogleSignInService {
  final GoogleSignIn _googleSignIn = GoogleSignIn(
    scopes: ['email'],
  );
  final FirebaseAuth _auth = FirebaseAuth.instance;

  /// Google 로그인
  Future<Map<String, dynamic>> signInWithGoogle() async {
    try {
      // Google 계정 선택
      final GoogleSignInAccount? googleUser = await _googleSignIn.signIn();

      if (googleUser == null) {
        return {
          "success": false,
          "message": "Google 로그인이 취소되었습니다."
        };
      }

      // 인증 정보 가져오기
      final GoogleSignInAuthentication googleAuth =
          await googleUser.authentication;

      // Firebase 인증
      final credential = GoogleAuthProvider.credential(
        accessToken: googleAuth.accessToken,
        idToken: googleAuth.idToken,
      );

      final userCredential = await _auth.signInWithCredential(credential);
      final user = userCredential.user;

      if (user != null) {
        return {
          "success": true,
          "message": "Google 로그인 성공",
          "user": {
            "email": user.email,
            "displayName": user.displayName,
            "photoUrl": user.photoURL,
            "uid": user.uid,
          }
        };
      }

      return {
        "success": false,
        "message": "로그인에 실패했습니다."
      };

    } catch (e) {
      return {
        "success": false,
        "message": "Google 로그인 오류: $e"
      };
    }
  }

  /// Google 로그아웃
  Future<void> signOut() async {
    await _googleSignIn.signOut();
    await _auth.signOut();
  }
}
```

---

### 2-2. Kakao 로그인

#### 📦 패키지 설치

```yaml
# pubspec.yaml
dependencies:
  kakao_flutter_sdk: ^1.7.0
```

#### ⚙️ Kakao 개발자 설정

1. [Kakao Developers](https://developers.kakao.com/) 접속
2. 애플리케이션 추가
3. 네이티브 앱 키 발급
4. 플랫폼 설정 (Android 패키지명, 키 해시 등록)

**`android/app/src/main/AndroidManifest.xml`**
```xml
<manifest>
    <application>
        <!-- Kakao SDK 초기화 -->
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="YOUR_KAKAO_APP_KEY" />
    </application>
</manifest>
```

#### 🔧 Kakao 로그인 서비스

**`lib/services/kakao_signin_service.dart`**
```dart
import 'package:kakao_flutter_sdk/kakao_flutter_sdk.dart';

class KakaoSignInService {

  /// Kakao SDK 초기화 (main.dart에서 호출)
  static void initialize(String appKey) {
    KakaoSdk.init(nativeAppKey: appKey);
  }

  /// Kakao 로그인
  Future<Map<String, dynamic>> signInWithKakao() async {
    try {
      // 카카오톡 설치 여부 확인
      if (await isKakaoTalkInstalled()) {
        // 카카오톡으로 로그인
        await UserApi.instance.loginWithKakaoTalk();
      } else {
        // 카카오 계정으로 로그인
        await UserApi.instance.loginWithKakaoAccount();
      }

      // 사용자 정보 가져오기
      User user = await UserApi.instance.me();

      return {
        "success": true,
        "message": "Kakao 로그인 성공",
        "user": {
          "id": user.id.toString(),
          "email": user.kakaoAccount?.email,
          "nickname": user.kakaoAccount?.profile?.nickname,
          "profileImageUrl": user.kakaoAccount?.profile?.profileImageUrl,
        }
      };

    } catch (e) {
      return {
        "success": false,
        "message": "Kakao 로그인 오류: $e"
      };
    }
  }

  /// Kakao 로그아웃
  Future<void> signOut() async {
    await UserApi.instance.logout();
  }
}
```

---

### 2-3. 로그인 화면에 소셜 로그인 버튼 추가

**`login_screen.dart` 수정**

```dart
// Google 로그인 버튼
ElevatedButton.icon(
  onPressed: () async {
    final result = await GoogleSignInService().signInWithGoogle();

    if (result['success']) {
      // 백엔드에 소셜 로그인 정보 전송 (회원가입 또는 로그인)
      final user = result['user'];
      // TODO: 백엔드 API 호출하여 회원 확인/생성

      Navigator.pushReplacementNamed(context, '/main');
    } else {
      ScaffoldMessenger.of(context).showSnackBar(
        SnackBar(content: Text(result['message'])),
      );
    }
  },
  icon: Icon(Icons.g_translate),  // Google 아이콘
  label: Text('Google로 로그인'),
  style: ElevatedButton.styleFrom(
    backgroundColor: Colors.white,
    foregroundColor: Colors.black87,
  ),
)

// Kakao 로그인 버튼
ElevatedButton(
  onPressed: () async {
    final result = await KakaoSignInService().signInWithKakao();

    if (result['success']) {
      // 백엔드 처리
      Navigator.pushReplacementNamed(context, '/main');
    }
  },
  child: Text('Kakao로 로그인'),
  style: ElevatedButton.styleFrom(
    backgroundColor: Color(0xFFFEE500),  // 카카오 노란색
    foregroundColor: Colors.black87,
  ),
)
```

---

## 🎯 통합 플로우

### 일반 회원가입 (이메일 인증)
1. 이메일 입력
2. **인증 코드 발송** ✉️
3. **인증 코드 입력 및 확인** ✅
4. 비밀번호 입력
5. 추가 정보 입력
6. 회원가입 완료

### 소셜 로그인
1. Google/Kakao 버튼 클릭
2. 소셜 계정 선택 및 인증
3. 백엔드에서 회원 확인
   - 기존 회원 → 로그인 완료
   - 신규 회원 → 추가 정보 입력 → 회원가입 완료

---

## 📌 체크리스트

### 이메일 인증
- [ ] 백엔드에 이메일 발송 라이브러리 추가
- [ ] Gmail/Naver SMTP 설정
- [ ] 인증 코드 발송 API 구현
- [ ] 인증 코드 확인 API 구현
- [ ] 프론트엔드 이메일 인증 서비스 구현
- [ ] 회원가입 플로우에 인증 단계 추가

### Google 로그인
- [ ] Firebase 프로젝트 설정
- [ ] google_sign_in 패키지 설치
- [ ] Android/iOS 설정 완료
- [ ] 로그인 서비스 구현
- [ ] 백엔드 소셜 로그인 API 구현

### Kakao 로그인
- [ ] Kakao Developers 앱 등록
- [ ] kakao_flutter_sdk 패키지 설치
- [ ] Android/iOS 설정 완료
- [ ] 로그인 서비스 구현
- [ ] 백엔드 소셜 로그인 API 구현

---

## 💡 추가 팁

1. **이메일 인증 코드 유효 시간**: Redis를 사용하면 TTL로 자동 만료 가능
2. **소셜 로그인 토큰 관리**: JWT 토큰으로 세션 관리
3. **보안**: 이메일 발송 시 Rate Limiting 적용 (스팸 방지)
4. **UX**: 인증 코드 입력 시 자동 완성 지원
5. **에러 처리**: 네트워크 오류, 타임아웃 등 예외 처리 필수
