# 간편 상담 기능 구현 완료

## ✅ 백엔드 구현 완료

### 1. Entity & Domain
- ✅ `ConsultationStatus.java` - 상담 상태 ENUM (PENDING, ANSWERED, CLOSED, CANCELLED)
- ✅ `Consultation.java` - 상담 엔티티
- ✅ `ConsultationRepository.java` - JPA Repository

### 2. DTO
- ✅ `ConsultationCreateReq.java` - 상담 생성 요청 DTO
- ✅ `ConsultationRes.java` - 상담 응답 DTO

### 3. Service & Controller
- ✅ `ConsultationService.java` - 비즈니스 로직
- ✅ `ConsultationController.java` - REST API 컨트롤러

### 4. 데이터베이스
- ✅ `create_consultations_table.sql` - 테이블 생성 SQL

---

## 📡 백엔드 API 엔드포인트

### 상담 생성
```
POST /api/v1/consultations
Content-Type: application/json

{
  "userId": 1,
  "partnerId": 5,
  "reservationId": null,  // 예약 없는 간편 상담
  "petType": "강아지",
  "subject": "건강 상담",
  "content": "강아지가 자꾸 기침을 하는데 괜찮을까요?",
  "imageUrl": null
}
```

### 사용자별 상담 목록 조회
```
GET /api/v1/consultations/user/{userId}
```

### 파트너별 상담 목록 조회
```
GET /api/v1/consultations/partner/{partnerId}
```

### 상담 상세 조회
```
GET /api/v1/consultations/{consultationId}
```

### 상담 답변 작성 (파트너용)
```
PUT /api/v1/consultations/{consultationId}/answer
Content-Type: text/plain

답변 내용...
```

### 상담 취소
```
DELETE /api/v1/consultations/{consultationId}
```

---

## 🗄️ 데이터베이스 테이블 생성

백엔드 서버를 재시작하기 전에 데이터베이스에 테이블을 생성해야 합니다:

```bash
# MySQL 접속
mysql -u root -p

# pet_service 데이터베이스 선택
use pet_service;

# 테이블 생성 SQL 실행
source c:\project\backend\demo\src\main\resources\create_consultations_table.sql
```

또는 직접 SQL 복사하여 실행:
```sql
CREATE TABLE IF NOT EXISTS consultations (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    partner_id BIGINT NOT NULL,
    reservation_id BIGINT NULL,
    pet_type VARCHAR(20) NOT NULL,
    subject VARCHAR(100) NOT NULL,
    content TEXT NOT NULL,
    image_url VARCHAR(500) NULL,
    answer TEXT NULL,
    answered_at DATETIME NULL,
    status ENUM('PENDING', 'ANSWERED', 'CLOSED', 'CANCELLED') NOT NULL DEFAULT 'PENDING',
    created_at DATETIME NOT NULL,

    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (partner_id) REFERENCES partners(id) ON DELETE CASCADE,
    FOREIGN KEY (reservation_id) REFERENCES reservations(id) ON DELETE SET NULL,

    INDEX idx_user_id (user_id),
    INDEX idx_partner_id (partner_id),
    INDEX idx_reservation_id (reservation_id),
    INDEX idx_status (status),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 🎯 다음 단계: 프론트엔드 구현

프론트엔드는 다음 파일들을 생성해야 합니다:

### 1. 모델 & 서비스
- `lib/models/consultation_model.dart` - 상담 모델
- `lib/services/consultation_service.dart` - API 서비스

### 2. UI 페이지
- 수의사 프로필 페이지의 "간편 상담" 버튼 수정
- `lib/pages/simple_consultation_page.dart` - 간편 상담 작성 페이지 (이미 있음)
- 수의사 찾기 화면의 "내 상담" 탭에 상담 목록 표시

---

## 🔧 백엔드 서버 재시작

백엔드 코드를 수정했으므로 서버를 재시작해야 합니다:

```bash
# 현재 실행중인 백엔드 서버 종료
# (포트 9075 사용 중인 프로세스 찾아서 종료)

# 백엔드 재시작
cd c:\project\backend\demo
.\gradlew.bat bootRun
```

---

## 📱 프론트엔드 사용 흐름

### 1. 간편 상담 작성
```
수의사 찾기 → 수의사 선택 → 프로필 상세 → 간편 상담 버튼 클릭
→ 상담 작성 페이지 → 제목, 내용, 반려동물 종류 입력 → 전송
```

### 2. 내 상담 확인
```
수의사 찾기 → 내 상담 탭 클릭
→ 내가 요청한 상담 목록 확인
→ 상담 선택 → 상담 내용 및 파트너 답변 확인
```

---

## 테이블 생성이 완료되면 알려주세요!

데이터베이스에 `consultations` 테이블을 생성한 후, 백엔드 서버를 재시작하면 프론트엔드 구현을 계속 진행하겠습니다.
