# 예약(Reservation)과 연동된 상담 기능 구현 완료 ✅

## 🎯 구현 내용

Reservation 테이블을 활용하여 **예약 완료 후 추가 상담**을 할 수 있는 기능을 구현했습니다.

---

## ✨ 주요 변경사항

### 1. **예약과 상담 연결**
- `Consultation` 엔티티에 `reservationId` 필드 추가 (선택적)
- 예약 없이도 간편 상담 가능
- 예약 후에도 해당 예약에 대한 추가 상담 가능

### 2. **완료된 예약에 "추가 상담" 버튼 추가**
- 예약 상태가 `COMPLETED`인 경우 "이 예약에 대해 추가 상담하기" 버튼 표시
- 버튼 클릭 시 해당 예약 정보와 함께 간편 상담 페이지로 이동
- 상담 생성 시 자동으로 `reservationId`가 포함되어 저장됨

---

## 📝 수정된 파일

### Backend

#### 1. **Consultation.java** (엔티티)
```java
/**
 * 연결된 예약 (선택적)
 * 예약 후 추가 상담인 경우에만 값이 있음
 */
@Column(name = "reservation_id")
private Long reservationId;
```

#### 2. **ConsultationCreateReq.java** (DTO)
```java
private Long reservationId;  // 예약 관련 상담인 경우 (선택적)
```

#### 3. **ConsultationRes.java** (DTO)
```java
private Long reservationId;  // 연결된 예약 ID (선택적)
```

#### 4. **ConsultationService.java**
- `createConsultation()` 메서드에서 `reservationId` 처리
- `toRes()` 메서드에서 `reservationId` 포함하여 응답

#### 5. **ConsultationRepository.java**
새로운 쿼리 메서드 추가:
```java
/**
 * 특정 예약에 대한 상담 조회
 */
List<Consultation> findByReservationId(Long reservationId);

/**
 * 특정 예약에 상담이 있는지 확인
 */
boolean existsByReservationId(Long reservationId);
```

### Frontend

#### 1. **consultation_record.dart** (모델)
```dart
final int? reservationId;  // 연결된 예약 ID (선택적)
```

#### 2. **consultation_service.dart**
```dart
Future<ConsultationRecord> createConsultation({
  required int userId,
  required int partnerId,
  int? reservationId,  // 예약 관련 상담인 경우 (선택적)
  required String petType,
  required String subject,
  required String content,
})
```

#### 3. **simple_consultation_page.dart**
```dart
class SimpleConsultationPage extends StatefulWidget {
  final dynamic partner;
  final int? reservationId; // 예약 관련 상담인 경우 (선택적)

  const SimpleConsultationPage({
    super.key,
    required this.partner,
    this.reservationId,  // 추가됨
  });
}
```

#### 4. **my_reservation.dart**
완료된 예약에 추가 상담 버튼 추가:

```dart
// 추가 상담 버튼 (완료된 예약인 경우)
if (reservation.status == 'COMPLETED') ...[
  const SizedBox(height: 12),
  SizedBox(
    width: double.infinity,
    child: OutlinedButton.icon(
      onPressed: () => _requestConsultation(context, reservation),
      icon: const Icon(Icons.chat_bubble_outline),
      label: const Text('이 예약에 대해 추가 상담하기'),
      style: OutlinedButton.styleFrom(
        foregroundColor: const Color(0xFF4FC59E),
        side: const BorderSide(color: Color(0xFF4FC59E)),
        padding: const EdgeInsets.symmetric(vertical: 12),
      ),
    ),
  ),
],
```

새로운 메서드 추가:
```dart
void _requestConsultation(BuildContext context, UserReservationModel reservation) async {
  // 예약 정보에서 파트너 정보를 동적으로 생성
  // SimpleConsultationPage로 이동하며 reservationId 전달
}
```

---

## 🗄️ 데이터베이스 테이블 수정 필요

**⚠️ 중요:** 기존 `consultations` 테이블에 `reservation_id` 컬럼을 추가해야 합니다.

### SQL 마이그레이션 파일 생성됨 ✅

마이그레이션 파일 위치: [db_migration_add_reservation_id_to_consultations.sql](backend/demo/src/main/resources/db_migration_add_reservation_id_to_consultations.sql)

### 실행 방법:

#### 방법 1: MySQL CLI에서 실행
```bash
mysql -u Insa6_aiservice_p3_2 -p -h project-db-campus.smhrd.com -P 3312 Insa6_aiservice_p3_2 < backend/demo/src/main/resources/db_migration_add_reservation_id_to_consultations.sql
```

#### 방법 2: MySQL Workbench / DBeaver에서 실행
1. 데이터베이스에 연결
   - Host: `project-db-campus.smhrd.com`
   - Port: `3312`
   - Database: `Insa6_aiservice_p3_2`
   - Username: `Insa6_aiservice_p3_2`
   - Password: `aischool2`
2. 아래 SQL 실행:

```sql
-- consultations 테이블에 reservation_id 컬럼 추가
ALTER TABLE consultations
ADD COLUMN reservation_id BIGINT NULL AFTER user_id
COMMENT '연결된 예약 ID (선택적)';

-- 인덱스 추가 (성능 최적화)
CREATE INDEX idx_reservation_id ON consultations(reservation_id);
```

3. 확인 쿼리로 컬럼 추가 검증:
```sql
SELECT
    COLUMN_NAME,
    COLUMN_TYPE,
    IS_NULLABLE,
    COLUMN_COMMENT
FROM
    INFORMATION_SCHEMA.COLUMNS
WHERE
    TABLE_SCHEMA = DATABASE()
    AND TABLE_NAME = 'consultations'
    AND COLUMN_NAME = 'reservation_id';
```

#### 방법 3: Spring Boot를 통한 자동 마이그레이션 (선택사항)
현재 `spring.jpa.hibernate.ddl-auto=none` 설정이므로 수동 실행을 권장합니다.

또는 테이블을 새로 생성하는 경우:

```sql
CREATE TABLE consultations (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    partner_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL,
    reservation_id BIGINT NULL,  -- 예약 연결 (선택적)
    pet_type VARCHAR(20) NOT NULL,
    subject VARCHAR(100) NOT NULL,
    content TEXT NOT NULL,
    answer TEXT,
    answered_at DATETIME,
    status VARCHAR(20) NOT NULL DEFAULT 'PENDING',
    created_at DATETIME NOT NULL,

    FOREIGN KEY (partner_id) REFERENCES partners(id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (reservation_id) REFERENCES reservations(id),  -- 예약 외래키

    INDEX idx_user_id (user_id),
    INDEX idx_partner_id (partner_id),
    INDEX idx_reservation_id (reservation_id),
    INDEX idx_status (status),
    INDEX idx_created_at (created_at)
);
```

---

## 🚀 사용 시나리오

### 시나리오 1: 예약 없이 간편 상담
1. 병원/펫시터 프로필에서 "간편 상담" 버튼 클릭
2. 상담 내용 작성
3. 제출 → `reservationId`는 `null`

### 시나리오 2: 예약 완료 후 추가 상담 ⭐ (새로 추가됨)
1. 사용자가 병원 예약을 하고 진료를 받음
2. 예약 상태가 `COMPLETED`로 변경됨
3. **"내 예약" 페이지에서 완료된 예약 확인**
4. **"이 예약에 대해 추가 상담하기" 버튼 클릭**
5. 간편 상담 페이지로 이동 (예약 정보 자동 연결)
6. 추가 질문 작성 (예: "처방받은 약 복용 후 이상 증상이 있어요")
7. 제출 → `reservationId`에 예약 ID가 저장됨

---

## 📊 데이터 흐름

### 예약 연동 상담 요청 시:
```
사용자 → "추가 상담" 버튼 클릭
  ↓
SimpleConsultationPage(
  partner: 예약의 파트너 정보,
  reservationId: 예약 ID  ← 핵심!
)
  ↓
ConsultationService.createConsultation(
  userId: 사용자 ID,
  partnerId: 파트너 ID,
  reservationId: 예약 ID,  ← 함께 전달됨
  petType: "강아지",
  subject: "추가 문의",
  content: "..."
)
  ↓
Backend에 저장:
- consultations 테이블에 reservation_id 포함
```

### 상담 내역 조회 시:
```
사용자 → 내 상담 내역
  ↓
ConsultationRecord {
  id: 1,
  reservationId: 123,  ← 어느 예약에 대한 상담인지 표시
  subject: "추가 문의",
  ...
}
```

---

## 💡 활용 예시

### 1. 상담 내역에서 예약 정보 표시
상담 상세 페이지에서 연결된 예약 정보를 보여줄 수 있습니다:

```dart
if (consultation.reservationId != null) {
  Text('예약 번호: ${consultation.reservationId}');
  // 또는 예약 상세로 이동하는 버튼 추가
}
```

### 2. 예약별 상담 내역 확인
특정 예약에 대한 모든 상담을 조회:

```dart
// 백엔드 API: GET /consultations/reservation/{reservationId}
// 프론트엔드: consultationService.getConsultationsByReservation(reservationId)
```

---

## ✅ 장점

1. **맥락 유지**: 예약과 상담이 연결되어 어떤 진료/서비스에 대한 질문인지 명확함
2. **유연성**: 예약 없이도 간편 상담 가능, 예약 후에도 추가 상담 가능
3. **추적 가능**: 예약 ID로 관련된 모든 상담을 조회 가능
4. **사용자 편의**: 완료된 예약에서 바로 추가 질문 가능

---

## 🔧 테스트 방법

### 1. 데이터베이스 준비 ⚠️ (필수)
위의 "데이터베이스 테이블 수정 필요" 섹션의 SQL 마이그레이션을 실행하세요.

**빠른 실행 (MySQL Workbench/DBeaver):**
```sql
-- 1. 컬럼 추가
ALTER TABLE consultations
ADD COLUMN reservation_id BIGINT NULL AFTER user_id
COMMENT '연결된 예약 ID (선택적)';

-- 2. 인덱스 추가
CREATE INDEX idx_reservation_id ON consultations(reservation_id);

-- 3. 확인
SHOW COLUMNS FROM consultations LIKE 'reservation_id';
```

### 2. 백엔드 서버 재시작
데이터베이스 마이그레이션 후 백엔드를 재시작하세요:
```bash
cd backend/demo
# 기존 서버 종료 후
./gradlew bootRun
```

또는 IDE에서 Spring Boot 애플리케이션을 재시작하세요.

### 3. 프론트엔드 실행
```bash
cd front/main
flutter run
```

### 4. 기능 테스트 체크리스트
- [ ] 데이터베이스에 `reservation_id` 컬럼 추가됨
- [ ] 백엔드 서버 정상 시작됨
- [ ] 프론트엔드 앱 정상 실행됨
- [ ] 예약을 하나 생성하고 상태를 `COMPLETED`로 변경
- [ ] "내 예약" 페이지에서 완료된 예약 확인
- [ ] "이 예약에 대해 추가 상담하기" 버튼 표시됨
- [ ] 버튼 클릭 시 상담 페이지로 이동됨
- [ ] 상담 내용 작성 후 제출 성공
- [ ] "내 상담 내역"에서 방금 작성한 상담 확인
- [ ] 상담에 `reservationId` 값이 포함되어 있는지 확인 (디버그 또는 백엔드 로그)

### 5. API 테스트 (선택사항)
Postman이나 cURL로 직접 테스트:

```bash
# 예약 연결된 상담 생성
curl -X POST http://localhost:9075/consultations \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 1,
    "partnerId": 2,
    "reservationId": 123,
    "petType": "DOG",
    "subject": "추가 문의",
    "content": "예약 후 궁금한 점이 있습니다."
  }'

# 특정 예약의 상담 조회
curl -X GET http://localhost:9075/consultations/reservation/123
```

---

## 📋 향후 개선 아이디어

1. **예약 정보 표시**: 상담 상세 페이지에서 연결된 예약 정보 표시
2. **예약별 상담 탭**: 예약 상세 페이지에 "상담 내역" 탭 추가
3. **알림**: 예약 관련 상담에 답변이 달리면 특별한 알림
4. **필터링**: 상담 내역에서 "예약 관련" / "일반 문의" 필터링

---

**구현 완료일:** 2025년 (오늘)
**작성자:** Claude Code Assistant

**핵심 변경:** Reservation과 Consultation을 `reservation_id`로 연결하여, 예약 완료 후 추가 상담 가능하게 함! 🎉
