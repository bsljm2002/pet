# 예약자 데이터 테이블 구성 가이드

## 개요
이 문서는 펫 케어 예약 시스템의 데이터베이스 테이블 구조와 예약 데이터 생성 방법을 설명합니다.

---

## 1. 핵심 테이블 구조

### 1.1 `reservation` 테이블 (예약 정보)

예약의 핵심 정보를 저장하는 테이블입니다.

| 컬럼명 | 타입 | NULL 허용 | 설명 | 예시 |
|--------|------|-----------|------|------|
| `id` | BIGINT | NO | 예약 ID (Primary Key) | 1 |
| `user_id` | BIGINT | NO | 예약한 일반 사용자 ID (FK → users.id) | 123 |
| `partner_id` | BIGINT | NO | 예약 받는 파트너 ID (FK → partners.id) | 45 |
| `pets_id` | BIGINT | NO | 예약한 펫 ID (FK → pets.id) | 78 |
| `service_categorical` | ENUM | NO | 서비스 유형 | `HOSPITAL`, `SITTER`, `GROOMING`, `CAFE` |
| `user_type` | ENUM | NO | 파트너 타입 (service_categorical과 동일) | `HOSPITAL`, `SITTER` |
| `status` | ENUM | NO | 예약 상태 | `WAITING`, `CONFIRMED`, `COMPLETED`, `CANCELLED_BY_USER`, `CANCELLED_BY_BIZ` |
| `created_at` | TIMESTAMP | NO | 예약 생성 시간 (예약 날짜/시간) | 2025-11-21 14:00:00 |
| `canceled_at` | DATETIME | YES | 예약 취소 시간 | 2025-11-21 15:30:00 |
| `resv_url` | VARCHAR(1024) | YES | 예약 이미지 URL | https://example.com/image.jpg |
| `resv_content` | TEXT | YES | 예약 요청사항 / 거절 사유 | "대형견이라 조심해주세요" |
| `vet_specialty` | VARCHAR(255) | YES | 수의사 전문분야 (CSV) | "INTERNAL_MEDICINE,SURGERY" |
| `petsitter_work` | VARCHAR(255) | YES | 펫시터 업무 (CSV) | "WALK,HYGIENE" |

#### 예약 상태 (ReservationStatus)
- `WAITING` : 예약 대기 중 (파트너 확인 필요)
- `CONFIRMED` : 예약 확정됨 (파트너가 수락)
- `CHECKED_IN` : 체크인 완료
- `COMPLETED` : 서비스 완료
- `CANCELLED_BY_USER` : 사용자가 취소
- `CANCELLED_BY_BIZ` : 파트너가 거절

#### 서비스 유형 (ServiceCategorical)
- `HOSPITAL` : 동물병원 진료
- `SITTER` : 펫시터 서비스
- `GROOMING` : 미용
- `CAFE` : 펫카페

---

### 1.2 `users` 테이블 (사용자 정보)

일반 사용자와 파트너 사용자 모두를 관리하는 테이블입니다.

| 컬럼명 | 타입 | NULL 허용 | 설명 | 예시 |
|--------|------|-----------|------|------|
| `id` | BIGINT | NO | 사용자 ID (Primary Key) | 1 |
| `username` | VARCHAR(20) | NO | 사용자명 | "홍길동" |
| `nickname` | VARCHAR(50) | NO | 닉네임 | "멍멍이집사" |
| `email` | VARCHAR(255) | NO | 이메일 | "user@example.com" |
| `password` | VARCHAR(255) | NO | 암호화된 비밀번호 | "$2a$10$..." |
| `gender` | VARCHAR(10) | NO | 성별 | `MALE`, `FEMALE` |
| `birthdate` | DATE | NO | 생년월일 | 1990-01-01 |
| `address` | VARCHAR(255) | NO | 주소 | "서울시 강남구" |
| `user_type` | ENUM | NO | 사용자 유형 | `GENERAL`, `HOSPITAL`, `SITTER` |
| `status` | ENUM | NO | 계정 상태 | `ACTIVE`, `LOCKED`, `DELETED` |
| `fcm_token` | VARCHAR(255) | YES | FCM 푸시 알림 토큰 | "eXXXXX..." |

#### 사용자 유형 (UserType)
- `GENERAL` : 일반 사용자 (펫 보호자)
- `HOSPITAL` : 병원 파트너
- `SITTER` : 펫시터 파트너
- `GROOMING` : 미용 파트너
- `CAFE` : 카페 파트너
- `SELLER` : 판매자

---

### 1.3 `partners` 테이블 (파트너 정보)

병원, 펫시터 등 서비스 제공자 정보를 저장하는 테이블입니다.

| 컬럼명 | 타입 | NULL 허용 | 설명 | 예시 |
|--------|------|-----------|------|------|
| `id` | BIGINT | NO | 파트너 ID (Primary Key) | 1 |
| `user_id` | BIGINT | NO | 파트너 사용자 ID (FK → users.id) | 456 |
| `partner_type` | ENUM | NO | 파트너 유형 | `HOSPITAL`, `SITTER` |
| `name` | VARCHAR(100) | NO | 병원명 또는 시터 서비스명 | "해피동물병원" |
| `doctor_name` | VARCHAR(50) | YES | 담당 의사명 또는 시터명 | "김수의" |
| `address` | VARCHAR(200) | NO | 주소 | "서울시 강남구 테헤란로 123" |
| `latitude` | DOUBLE | YES | 위도 | 37.123456 |
| `longitude` | DOUBLE | YES | 경도 | 127.123456 |
| `phone` | VARCHAR(20) | NO | 전화번호 | "02-1234-5678" |
| `rating` | DOUBLE | YES | 평점 (0.0 ~ 5.0) | 4.5 |

---

### 1.4 `pets` 테이블 (펫 정보)

사용자가 등록한 반려동물 정보를 저장하는 테이블입니다.

| 컬럼명 | 타입 | NULL 허용 | 설명 | 예시 |
|--------|------|-----------|------|------|
| `id` | BIGINT | NO | 펫 ID (Primary Key) | 1 |
| `user_id` | BIGINT | NO | 소유자 ID (FK → users.id) | 123 |
| `p_name` | VARCHAR(20) | NO | 펫 이름 | "뽀삐" |
| `species` | ENUM | NO | 종 | `DOG`, `CAT` |
| `species_detail` | VARCHAR(30) | YES | 품종 상세 | "골든 리트리버" |
| `gender` | ENUM | NO | 성별 | `MALE`, `FEMALE` |
| `p_birthdate` | DATE | NO | 생년월일 | 2020-03-15 |
| `weight` | DECIMAL(4,1) | NO | 체중 (kg) | 15.5 |
| `abit_type_code` | ENUM | NO | MBTI 성격 유형 | `ENFP` |
| `p_image_url` | VARCHAR(2048) | YES | 펫 사진 URL | "https://..." |

---

## 2. 예약 데이터 생성 가이드

### 2.1 필수 선행 조건

예약을 생성하기 전에 다음 데이터가 반드시 존재해야 합니다:

1. **일반 사용자 계정** (`users` 테이블)
   - `user_type = 'GENERAL'`
   - `status = 'ACTIVE'`

2. **파트너 프로필** (`partners` 테이블)
   - 해당 `user_id`와 연결된 파트너 정보
   - `partner_type` 지정 (`HOSPITAL` 또는 `SITTER`)

3. **펫 정보** (`pets` 테이블)
   - 일반 사용자가 등록한 반려동물

---

### 2.2 예약 생성 SQL 예시

#### 병원 예약 생성
```sql
INSERT INTO reservation (
    user_id,
    partner_id,
    pets_id,
    service_categorical,
    user_type,
    status,
    created_at,
    resv_content,
    vet_specialty
) VALUES (
    123,                          -- 일반 사용자 ID
    45,                           -- 병원 파트너 ID
    78,                           -- 펫 ID
    'HOSPITAL',                   -- 서비스 유형
    'HOSPITAL',                   -- 파트너 타입
    'WAITING',                    -- 예약 상태 (대기중)
    '2025-11-25 10:00:00',       -- 예약 날짜/시간
    '피부가 가려워하는 것 같아요. 피부과 진료 부탁드립니다.',  -- 요청사항
    'DERMATOLOGY'                 -- 진료 과목 (피부과)
);
```

#### 펫시터 예약 생성
```sql
INSERT INTO reservation (
    user_id,
    partner_id,
    pets_id,
    service_categorical,
    user_type,
    status,
    created_at,
    resv_content,
    petsitter_work
) VALUES (
    123,                          -- 일반 사용자 ID
    67,                           -- 펫시터 파트너 ID
    78,                           -- 펫 ID
    'SITTER',                     -- 서비스 유형
    'SITTER',                     -- 파트너 타입
    'WAITING',                    -- 예약 상태 (대기중)
    '2025-11-25 14:00:00',       -- 예약 날짜/시간
    '산책 시 대형견이라 힘이 세니 조심해주세요.',  -- 요청사항
    'WALK,TRANSPORT'              -- 펫시터 업무 (산책, 이동)
);
```

---

### 2.3 예약 데이터 생성 프로세스

#### 프론트엔드 → 백엔드 API 호출

**API 엔드포인트**: `POST /api/v1/reservations`

**요청 본문 (ReservationCreateReq)**:
```json
{
  "userId": 123,
  "partnerId": 45,
  "petId": 78,
  "userType": "HOSPITAL",
  "reservationContent": "피부가 가려워하는 것 같아요. 피부과 진료 부탁드립니다.",
  "vetSpecialties": ["DERMATOLOGY"],
  "petsitterWorks": null,
  "reservationImageUrls": ["https://example.com/image1.jpg"],
  "createdAt": "2025-11-25T10:00:00+09:00"
}
```

**응답**:
```json
{
  "ok": true,
  "data": {
    "reservation_id": 1
  },
  "error": null,
  "message": null
}
```

---

## 3. 파트너 예약 조회 API

### 3.1 파트너가 받은 예약 목록 조회

**엔드포인트**: `GET /api/v1/reservations/partner?partnerId={partnerId}`

**응답 예시**:
```json
{
  "ok": true,
  "data": [
    {
      "reservationId": 1,
      "serviceType": "HOSPITAL",
      "slotLabel": "오전",
      "date": "2025.11.25",
      "hour": 10,
      "partnerName": "홍길동",
      "specialties": ["DERMATOLOGY"],
      "status": "WAITING"
    }
  ],
  "error": null,
  "message": null
}
```

---

### 3.2 예약 확정 (파트너가 수락)

**엔드포인트**: `PATCH /api/v1/reservations/{id}/accept?partner_id={partnerId}`

**효과**:
- 예약 상태가 `WAITING` → `CONFIRMED`로 변경
- 일반 사용자에게 FCM 푸시 알림 전송 (예약 확정 알림)

---

### 3.3 예약 거절 (파트너가 거부)

**엔드포인트**: `PATCH /api/v1/reservations/{id}/reject?partnerId={partnerId}&reason={거절사유}`

**효과**:
- 예약 상태가 `WAITING` → `CANCELLED_BY_BIZ`로 변경
- `canceled_at` 시간 기록
- 거절 사유가 `resv_content`에 "[거절 사유] ..." 형태로 추가됨

**요청 예시**:
```
PATCH /api/v1/reservations/1/reject?partnerId=45&reason=예약이%20마감되었습니다
```

---

## 4. 예약 데이터 샘플

### 4.1 테스트용 데이터 세트

아래는 테스트를 위한 샘플 데이터입니다.

#### 일반 사용자 생성
```sql
INSERT INTO users (username, nickname, email, password, gender, birthdate, address, user_type, status)
VALUES ('홍길동', '멍멍이집사', 'user@test.com', '$2a$10$dummyPassword', 'MALE', '1990-01-01', '서울시 강남구', 'GENERAL', 'ACTIVE');
-- user_id: 1 (예시)
```

#### 파트너 사용자 생성
```sql
INSERT INTO users (username, nickname, email, password, gender, birthdate, address, user_type, status)
VALUES ('김수의', '해피병원', 'vet@test.com', '$2a$10$dummyPassword', 'FEMALE', '1985-05-15', '서울시 서초구', 'HOSPITAL', 'ACTIVE');
-- user_id: 2 (예시)
```

#### 파트너 프로필 생성
```sql
INSERT INTO partners (user_id, partner_type, name, doctor_name, address, latitude, longitude, phone, rating)
VALUES (2, 'HOSPITAL', '해피동물병원', '김수의', '서울시 서초구 반포대로 123', 37.5012, 127.0030, '02-1234-5678', 4.5);
-- partner_id: 1 (예시)
```

#### 펫 생성
```sql
INSERT INTO pets (user_id, p_name, species, species_detail, gender, p_birthdate, weight, abit_type_code)
VALUES (1, '뽀삐', 'DOG', '골든 리트리버', 'MALE', '2020-03-15', 25.5, 'ENFP');
-- pet_id: 1 (예시)
```

#### 예약 생성 (대기중)
```sql
INSERT INTO reservation (user_id, partner_id, pets_id, service_categorical, user_type, status, created_at, resv_content, vet_specialty)
VALUES (1, 1, 1, 'HOSPITAL', 'HOSPITAL', 'WAITING', NOW(), '피부 검진 부탁드립니다.', 'DERMATOLOGY');
-- reservation_id: 1 (예시)
```

---

## 5. 데이터 확인 쿼리

### 5.1 특정 파트너의 예약 목록 조회
```sql
SELECT
    r.id AS reservation_id,
    r.status,
    r.created_at,
    u.username AS customer_name,
    p.p_name AS pet_name,
    r.resv_content
FROM reservation r
JOIN users u ON r.user_id = u.id
JOIN pets p ON r.pets_id = p.id
WHERE r.partner_id = 1  -- 파트너 ID
ORDER BY r.created_at DESC;
```

### 5.2 대기중인 예약 개수 확인
```sql
SELECT
    partner_id,
    COUNT(*) AS waiting_count
FROM reservation
WHERE status = 'WAITING'
GROUP BY partner_id;
```

### 5.3 특정 사용자의 모든 예약 조회
```sql
SELECT
    r.id,
    r.status,
    r.created_at,
    part.name AS partner_name,
    p.p_name AS pet_name
FROM reservation r
JOIN partners part ON r.partner_id = part.id
JOIN pets p ON r.pets_id = p.id
WHERE r.user_id = 1  -- 사용자 ID
ORDER BY r.created_at DESC;
```

---

## 6. 주의사항

### 6.1 데이터 무결성
- `user_id`, `partner_id`, `pets_id`는 반드시 존재하는 ID여야 합니다 (Foreign Key 제약)
- 파트너의 `user_type`과 `partner_type`은 일치해야 합니다
- 예약의 `service_categorical`과 `user_type`은 일치해야 합니다

### 6.2 날짜/시간
- `created_at`은 `OffsetDateTime` 타입으로 타임존 정보를 포함합니다
- 프론트엔드에서는 ISO 8601 형식(`2025-11-25T10:00:00+09:00`)으로 전달합니다

### 6.3 CSV 필드
- `vet_specialty`와 `petsitter_work`는 CSV 형태로 저장됩니다
- 예: `"DERMATOLOGY,INTERNAL_MEDICINE"`
- 백엔드에서 자동으로 List를 CSV로 변환합니다

### 6.4 예약 상태 흐름
```
WAITING (대기중)
   ↓
   ├→ CONFIRMED (확정) → CHECKED_IN (체크인) → COMPLETED (완료)
   │
   ├→ CANCELLED_BY_USER (사용자 취소)
   │
   └→ CANCELLED_BY_BIZ (파트너 거절)
```

---

## 7. 트러블슈팅

### 7.1 예약이 표시되지 않는 경우

**증상**: 파트너 예약 화면에 예약이 표시되지 않음

**원인 체크리스트**:
1. ✅ 백엔드 서버가 실행 중인지 확인 (포트 9075)
2. ✅ 데이터베이스에 예약 데이터가 존재하는지 확인
3. ✅ `partner_id`가 올바른지 확인
4. ✅ 프론트엔드 API 호출 시 올바른 `partnerId`를 전달하는지 확인

**해결 방법**:
```sql
-- 1. 예약 데이터 존재 확인
SELECT * FROM reservation WHERE partner_id = {your_partner_id};

-- 2. 예약이 없다면 테스트 데이터 생성
INSERT INTO reservation (...) VALUES (...);

-- 3. API 테스트
curl "http://localhost:9075/api/v1/reservations/partner?partnerId=1"
```

---

## 8. 관련 파일

### 백엔드
- [Reservation.java](backend/demo/src/main/java/com/example/pet/demo/reservation/domain/Reservation.java) - 예약 엔티티
- [ReservationService.java](backend/demo/src/main/java/com/example/pet/demo/reservation/app/ReservationService.java) - 예약 비즈니스 로직
- [ReservationController.java](backend/demo/src/main/java/com/example/pet/demo/reservation/api/ReservationController.java) - 예약 API 컨트롤러
- [ReservationRepository.java](backend/demo/src/main/java/com/example/pet/demo/reservation/domain/ReservationRepository.java) - 예약 데이터 접근

### 프론트엔드
- [partner_reservation_service.dart](front/main/lib/services/partner_reservation_service.dart) - 파트너 예약 API 서비스
- [partner_reservation_model.dart](front/main/lib/models/partner_reservation_model.dart) - 예약 데이터 모델
- [hospital_reservation_screen.dart](front/main/lib/screens/partner/hospital_reservation_screen.dart) - 병원 예약 화면
- [sitter_reservation_screen.dart](front/main/lib/screens/partner/sitter_reservation_screen.dart) - 펫시터 예약 화면

---

## 9. API 엔드포인트 요약

| 메서드 | 엔드포인트 | 설명 | 권한 |
|--------|-----------|------|------|
| POST | `/api/v1/reservations` | 예약 생성 | 일반 사용자 |
| GET | `/api/v1/reservations/my?userId={userId}&serviceType={type}` | 내 예약 목록 조회 | 일반 사용자 |
| GET | `/api/v1/reservations/partner?partnerId={partnerId}` | 파트너가 받은 예약 목록 | 파트너 |
| PATCH | `/api/v1/reservations/{id}/accept?partner_id={partnerId}` | 예약 확정 | 파트너 |
| PATCH | `/api/v1/reservations/{id}/reject?partnerId={partnerId}&reason={reason}` | 예약 거절 | 파트너 |
| GET | `/api/v1/reservations/completed/hospital?userId={userId}` | 완료된 병원 예약 | 일반 사용자 |
| GET | `/api/v1/reservations/completed/sitter?userId={userId}` | 완료된 펫시터 예약 | 일반 사용자 |

---

## 문의사항

예약 시스템과 관련하여 문제가 발생하거나 추가 기능이 필요한 경우, 개발팀에 문의하시기 바랍니다.

**작성일**: 2025-11-21
**버전**: 1.0
