# 상담 기능 구현 완료 ✅

## 구현 내용

예약과 **완전히 독립적인** 상담 기능이 별도 테이블로 구현되었습니다.

### 백엔드 구현 완료 사항

#### 1. 데이터베이스
- **테이블명**: `consultations`
- **위치**: `backend/demo/src/main/resources/create_consultation_table.sql`
- **구조**:
  - id: 상담 ID (PK, AUTO_INCREMENT)
  - user_id: 고객 ID
  - partner_id: 파트너 ID
  - reservation_id: 연결된 예약 ID (선택적, NULL 가능)
  - pet_type: 반려동물 종류
  - subject: 상담 제목
  - content: 상담 내용
  - answer: 파트너 답변
  - answered_at: 답변 일시
  - status: 상담 상태 (PENDING/ANSWERED/CANCELLED)
  - created_at: 생성 일시

#### 2. 백엔드 코드
✅ **Consultation.java** - JPA Entity
✅ **ConsultationRepository.java** - 데이터 접근 계층
✅ **ConsultationService.java** - 비즈니스 로직
✅ **ConsultationController.java** - REST API 엔드포인트
✅ **ConsultationStatus.java** - 상담 상태 Enum
✅ **DTO 클래스들** (ConsultationCreateReq, ConsultationRes, AnswerCreateReq)

#### 3. API 엔드포인트 (ApiResponse 래퍼 적용)

| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/api/v1/consultations` | 상담 생성 |
| GET | `/api/v1/consultations/{id}` | 특정 상담 조회 |
| GET | `/api/v1/consultations/user/{userId}` | 사용자의 모든 상담 조회 |
| GET | `/api/v1/consultations/partner/{partnerId}` | 파트너의 모든 상담 조회 |
| POST | `/api/v1/consultations/{id}/answer` | 파트너가 답변 추가 |
| DELETE | `/api/v1/consultations/{id}` | 상담 취소 |

### ⚠️ 데이터베이스 마이그레이션 필요

서버는 정상적으로 시작되었지만, **consultations 테이블이 아직 생성되지 않았습니다**.

다음 SQL 파일을 실행해주세요:
```
backend/demo/src/main/resources/create_consultation_table.sql
```

#### MySQL 접속 정보
- Host: project-db-campus.smhrd.com
- Port: 3312
- Database: Insa6_aiservice_p3_2
- Username: Insa6_aiservice_p3_2
- Password: aischool2

#### 실행 방법

**방법 1: DBeaver/MySQL Workbench 등 GUI 툴 사용**
1. 위 접속 정보로 데이터베이스 연결
2. `create_consultation_table.sql` 파일 내용을 복사
3. 쿼리 실행

**방법 2: MySQL CLI (설치되어 있는 경우)**
```bash
mysql -h project-db-campus.smhrd.com -P 3312 -u Insa6_aiservice_p3_2 -p Insa6_aiservice_p3_2 < backend/demo/src/main/resources/create_consultation_table.sql
```

### API 테스트 예시

#### 1. 상담 생성
```bash
curl -X POST http://localhost:9075/api/v1/consultations \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 1,
    "partnerId": 1,
    "petType": "강아지",
    "subject": "피부병 문의",
    "content": "강아지 피부에 붉은 반점이 생겼어요"
  }'
```

#### 2. 사용자의 상담 목록 조회
```bash
curl http://localhost:9075/api/v1/consultations/user/1
```

#### 3. 파트너가 답변 추가
```bash
curl -X POST http://localhost:9075/api/v1/consultations/1/answer \
  -H "Content-Type: application/json" \
  -d '{
    "answer": "피부병은 다양한 원인이 있을 수 있습니다. 직접 진료를 받아보시는 것을 권장드립니다."
  }'
```

### 프론트엔드 구현 예정

다음 단계로 Flutter 프론트엔드에서 다음을 구현해야 합니다:

1. **ConsultationService** (API 호출)
2. **간편 상담 페이지** (파트너 프로필에서 접근)
   - 상담 작성 폼
   - 내 상담 목록
   - 상담 상세 및 답변 확인
3. **파트너용 상담 관리 페이지**
   - 받은 상담 목록
   - 답변 작성

### 주의사항

✅ **기존 기능과 완전히 독립적**
- 예약(reservation) 테이블과 분리됨
- 리뷰 기능에 영향 없음
- 예약 기능에 영향 없음

✅ **별도 테이블 사용의 장점**
- 더 명확한 데이터 구조
- 독립적인 쿼리 최적화
- 예약 없이도 상담 가능 (진정한 "간편 상담")

### 서버 상태

✅ 서버 정상 실행 중 (Port 9075)
✅ 빌드 성공
✅ JPA Repository 9개 인식 (ConsultationRepository 포함)
⏳ 데이터베이스 테이블 생성 대기 중
