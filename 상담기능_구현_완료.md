# 간편 상담 기능 구현 완료

## 📋 구현 내용 요약

사용자가 병원(수의사)이나 펫시터에게 간편하게 상담을 요청하고, 상담 내역을 확인하며, 파트너의 답변을 볼 수 있는 기능을 구현했습니다.

---

## 🎯 주요 기능

### 1. **간편 상담 신청**
- 병원 또는 펫시터 프로필 페이지에서 "간편 상담" 버튼 클릭
- 다음 정보만 입력하여 간단하게 상담 신청:
  - 반려동물 종류 (강아지/고양이)
  - 문의 주제 (최대 50자)
  - 문의 내용 (10~300자)

### 2. **내 상담 내역**
- 신청한 모든 상담을 시간순으로 리스트 표시
- 각 상담의 상태 확인:
  - 🟠 답변 대기중 (PENDING)
  - 🟢 답변 완료 (ANSWERED)
  - 🔴 취소됨 (CANCELLED)
- 답변이 달린 상담은 "답변있음" 배지 표시

### 3. **상담 상세 보기**
- 상담 내용 전체 확인
- 파트너가 답변한 경우 답변 내용 표시
- 답변 일시 표시

---

## 📁 구현된 파일

### Backend (Java/Spring Boot)

#### 1. **Entity & Domain**
- `Consultation.java` - 상담 엔티티
  - 필드: 사용자, 파트너, 반려동물종류, 주제, 내용, 답변, 상태 등
  - 메서드: `addAnswer()`, `cancel()`

- `ConsultationStatus.java` - 상담 상태 Enum
  - PENDING (답변 대기)
  - ANSWERED (답변 완료)
  - CANCELLED (취소됨)

- `ConsultationRepository.java` - JPA Repository
  - `findByUserIdOrderByCreatedAtDesc()` - 사용자의 상담 조회
  - `findByPartnerIdOrderByCreatedAtDesc()` - 파트너의 상담 조회
  - `countPendingByUserId()` - 대기중인 상담 개수
  - `countPendingByPartnerId()` - 파트너의 대기 상담 개수

#### 2. **DTO**
- `ConsultationCreateReq.java` - 상담 생성 요청
- `ConsultationRes.java` - 상담 응답
- `AnswerCreateReq.java` - 답변 추가 요청

#### 3. **Service & Controller**
- `ConsultationService.java` - 비즈니스 로직
  - 상담 생성, 조회, 답변 추가, 취소

- `ConsultationController.java` - REST API
  ```
  POST   /consultations              - 상담 생성
  GET    /consultations/{id}         - 상담 조회
  GET    /consultations/user/{userId}      - 사용자 상담 목록
  GET    /consultations/partner/{partnerId} - 파트너 상담 목록
  POST   /consultations/{id}/answer  - 답변 추가
  DELETE /consultations/{id}          - 상담 취소
  ```

### Frontend (Flutter/Dart)

#### 1. **Models**
- `consultation_record.dart` - 상담 데이터 모델
  - JSON 변환 (`fromJson`)
  - 상태 텍스트 변환 (`statusText`)
  - 답변 여부 확인 (`hasAnswer`)

#### 2. **Services**
- `consultation_service.dart` - API 통신 서비스
  - `createConsultation()` - 상담 생성
  - `getUserConsultations()` - 사용자 상담 목록
  - `getConsultation()` - 상담 상세 조회
  - `addAnswer()` - 답변 추가
  - `cancelConsultation()` - 상담 취소

#### 3. **Pages**
- `simple_consultation_page.dart` - 간편 상담 신청 페이지
  - 반려동물 종류 선택 (강아지/고양이)
  - 문의 주제 입력 (50자 제한)
  - 문의 내용 입력 (10~300자)
  - API 호출 및 로딩 상태 처리

- `my_consultations_page.dart` - 내 상담 내역 페이지
  - 상담 리스트 표시
  - 상태별 색상 구분
  - Pull-to-refresh 기능
  - 답변 있음 배지 표시

- `consultation_detail_page_new.dart` - 상담 상세 페이지
  - 상담 정보 전체 표시
  - 파트너 정보
  - 문의 내용
  - 답변 내용 (있는 경우)

---

## 🗄️ 데이터베이스 테이블 생성 필요

**⚠️ 중요:** 백엔드 서버를 실행하기 전에 데이터베이스에 `consultations` 테이블을 생성해야 합니다.

### SQL 스크립트:

```sql
CREATE TABLE consultations (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    partner_id BIGINT NOT NULL,
    user_id BIGINT NOT NULL,
    pet_type VARCHAR(20) NOT NULL,
    subject VARCHAR(100) NOT NULL,
    content TEXT NOT NULL,
    answer TEXT,
    answered_at DATETIME,
    status VARCHAR(20) NOT NULL DEFAULT 'PENDING',
    created_at DATETIME NOT NULL,

    FOREIGN KEY (partner_id) REFERENCES partners(id),
    FOREIGN KEY (user_id) REFERENCES users(id),

    INDEX idx_user_id (user_id),
    INDEX idx_partner_id (partner_id),
    INDEX idx_status (status),
    INDEX idx_created_at (created_at)
);
```

### 테이블 생성 방법:

```sql
-- MySQL에 접속
mysql -u root -p

-- pet 데이터베이스 선택
USE pet;

-- 위의 CREATE TABLE 스크립트 실행
```

---

## 🚀 사용 방법

### 1. 데이터베이스 설정
```bash
# MySQL 접속
mysql -u root -p

# 데이터베이스 선택
USE pet;

# consultations 테이블 생성 (위의 SQL 스크립트 실행)
```

### 2. 백엔드 서버 재시작
```bash
cd backend/demo
./gradlew bootRun
```

### 3. 프론트엔드 실행 (이미 실행중이면 Hot Reload 자동 적용)
```bash
cd front/main
flutter run
```

### 4. 기능 테스트

#### (1) 간편 상담 신청
1. 앱에서 병원 목록 또는 펫시터 목록으로 이동
2. 특정 병원/펫시터의 프로필 페이지 열기
3. "간편 상담" 버튼 클릭
4. 정보 입력:
   - 반려동물 종류 선택 (강아지/고양이)
   - 문의 주제 입력 (예: "예방접종 문의")
   - 문의 내용 입력 (예: "강아지 종합백신 가격이 궁금합니다")
5. "상담 요청하기" 버튼 클릭
6. 성공 메시지 확인

#### (2) 상담 내역 확인
1. 앱에서 "내 상담 내역" 메뉴로 이동
   - (이 메뉴는 사용자가 적절한 곳에 추가해야 합니다)
2. 신청한 상담 리스트 확인
3. 특정 상담을 탭하여 상세 내용 확인

#### (3) 파트너 답변 확인 (답변이 달린 후)
1. 내 상담 내역에서 "답변있음" 배지가 표시된 상담 클릭
2. 하단에 파트너 답변 섹션 확인

---

## 🔗 통합 방법

### 메뉴에 "내 상담 내역" 추가

사용자가 상담 내역을 쉽게 확인할 수 있도록 네비게이션 메뉴에 추가해주세요:

```dart
// 예시: BottomNavigationBar 또는 Drawer에 추가
ListTile(
  leading: Icon(Icons.chat_bubble_outline),
  title: Text('내 상담 내역'),
  onTap: () {
    Navigator.push(
      context,
      MaterialPageRoute(
        builder: (context) => MyConsultationsPage(),
      ),
    );
  },
),
```

### 병원/펫시터 프로필에서 간편 상담 버튼 연결

병원이나 펫시터 프로필 페이지에 "간편 상담" 버튼을 추가하고 `SimpleConsultationPage`로 연결:

```dart
ElevatedButton(
  onPressed: () {
    Navigator.push(
      context,
      MaterialPageRoute(
        builder: (context) => SimpleConsultationPage(
          partner: vetOrSitterModel, // VetModel 또는 SitterModel 전달
        ),
      ),
    );
  },
  child: Text('간편 상담'),
),
```

---

## 📊 파트너용 기능 (추후 구현 가능)

현재는 사용자(일반 회원)가 상담을 신청하고 내역을 보는 기능만 구현되어 있습니다.

파트너(병원/펫시터)가 답변을 작성할 수 있는 기능은 별도로 구현해야 합니다:

### 파트너용 화면 예시:
1. **받은 상담 목록** - 자신에게 온 상담 요청 리스트
2. **상담 답변 작성** - 상담에 대한 답변 입력
3. **답변 완료 상담** - 답변한 상담 내역

### API는 이미 준비됨:
- `GET /consultations/partner/{partnerId}` - 파트너가 받은 상담 목록
- `POST /consultations/{id}/answer` - 답변 추가

---

## ✅ 완료된 작업

- [x] 백엔드 Consultation Entity 생성
- [x] ConsultationRepository 인터페이스 생성
- [x] ConsultationService 비즈니스 로직 구현
- [x] ConsultationController REST API 구현
- [x] 프론트엔드 ConsultationRecord 모델 생성
- [x] ConsultationService API 통신 구현
- [x] 상담 내역 리스트 페이지 생성
- [x] 상담 상세 페이지 생성
- [x] 간편 상담 신청 페이지 API 연결

---

## 🎨 UI 특징

### 색상 테마
- 주요 색상: `#4FC59E` (민트 그린)
- 배경 색상: `#E6F7F1` (연한 민트)
- 강조 색상: `#003829` (짙은 녹색)

### 상태별 색상
- 답변 대기중 (PENDING): 🟠 주황색
- 답변 완료 (ANSWERED): 🟢 민트 그린
- 취소됨 (CANCELLED): 🔴 빨간색

### UI 컴포넌트
- 카드 형식의 리스트 아이템
- 둥근 모서리 (16px)
- 그림자 효과로 입체감
- 답변 있는 경우 초록색 테두리 강조

---

## 🐛 주의사항

1. **데이터베이스 테이블 생성 필수**
   - 위의 SQL 스크립트를 실행하여 `consultations` 테이블을 먼저 생성해야 합니다.

2. **외래 키 제약**
   - `partner_id`는 `partners` 테이블의 `id`를 참조
   - `user_id`는 `users` 테이블의 `id`를 참조

3. **로그인 필요**
   - 상담 신청 및 내역 조회는 로그인된 사용자만 가능합니다.

4. **파트너 ID 형식**
   - 현재 VetModel과 SitterModel의 id가 String 타입이므로, int로 변환하여 API에 전달합니다.

---

## 📝 향후 개선 사항

1. **푸시 알림**
   - 답변이 달렸을 때 사용자에게 푸시 알림 전송

2. **이미지 첨부**
   - 상담 신청 시 반려동물 사진 첨부 가능

3. **파트너 답변 UI**
   - 파트너용 상담 관리 화면 구현

4. **상담 필터링**
   - 상태별(대기중/완료/취소) 필터링 기능

5. **검색 기능**
   - 상담 내역에서 키워드 검색

---

**구현 완료일:** 2025년 (오늘)
**작성자:** Claude Code Assistant
