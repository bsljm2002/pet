# `consultations` 테이블 완전 가이드 📋

## 📌 테이블 목적

**예약 없이도 사용 가능한 독립적인 간편 상담 기능**

사용자가 병원/펫시터에게 간단한 질문과 **사진**을 첨부하여 상담할 수 있습니다.

---

## 📊 테이블 구조

### 전체 컬럼 목록

| 컬럼명 | 타입 | 필수 | 설명 |
|--------|------|------|------|
| **id** | BIGINT | ✅ | 상담 고유 번호 (자동 증가) |
| **user_id** | BIGINT | ✅ | 상담 요청 고객 ID (users 테이블) |
| **partner_id** | BIGINT | ✅ | 상담 받는 파트너 ID (partner 테이블) |
| **reservation_id** | BIGINT | ❌ | 예약 연결 ID (선택적) |
| **pet_type** | VARCHAR(20) | ✅ | 반려동물 종류 (예: "강아지", "고양이") |
| **subject** | VARCHAR(100) | ✅ | 상담 제목 |
| **content** | TEXT | ✅ | 상담 내용 상세 |
| **image_url** | VARCHAR(500) | ❌ | 📷 **첨부 이미지 URL** (선택적) |
| **answer** | TEXT | ❌ | 파트너 답변 |
| **answered_at** | DATETIME | ❌ | 답변 일시 |
| **status** | VARCHAR(20) | ✅ | 상담 상태 (기본값: PENDING) |
| **created_at** | DATETIME | ✅ | 상담 요청 일시 |

---

## 🔍 컬럼 상세 설명

### 1️⃣ 기본 정보

**id** (Primary Key)
- 상담 고유 번호
- 자동 증가 (AUTO_INCREMENT)
- 각 상담마다 고유한 번호

**created_at**
- 고객이 상담을 요청한 날짜/시간
- 자동 저장

---

### 2️⃣ 관계 정보

**user_id** (Foreign Key → users 테이블)
- 상담을 요청한 고객의 ID
- 예: user_id = 5 (고객 "홍길동")

**partner_id** (Foreign Key → partner 테이블)
- 상담을 받는 파트너(병원/펫시터) ID
- 예: partner_id = 3 (서울동물병원)

**reservation_id** (선택적)
- 예약과 연결된 상담인 경우에만 값 있음
- NULL 가능 (예약 없이 단독 상담 가능)

---

### 3️⃣ 상담 내용

**pet_type** (필수)
- 상담 대상 반려동물 종류
- 예: "강아지", "고양이"
- 최대 20자

**subject** (필수)
- 상담 제목/주제
- 예: "예방접종 문의", "피부병 상담"
- 최대 100자

**content** (필수)
- 상담 내용 상세
- 고객이 작성한 질문
- TEXT 타입 (긴 내용 가능)

**image_url** 📷 (선택적) - **NEW!**
- 첨부된 이미지 파일의 URL
- 예: "http://server.com/uploads/consultation_123.jpg"
- 최대 500자
- NULL 가능 (사진 없이도 상담 가능)
- **사용 예시:**
  - 피부병 증상 사진
  - 반려동물 상태 사진
  - 의심 부위 사진 등

---

### 4️⃣ 답변 정보

**answer** (선택적)
- 파트너가 작성한 답변
- 아직 답변 없으면 NULL
- TEXT 타입

**answered_at** (선택적)
- 파트너가 답변한 날짜/시간
- 답변 전에는 NULL

---

### 5️⃣ 상태 관리

**status** (필수)
- 상담의 현재 상태
- 기본값: **PENDING** (답변 대기중)

**status 값 설명:**

| 값 | 한글 | 의미 |
|----|------|------|
| **PENDING** | 답변 대기중 | 고객이 상담 요청함, 파트너 답변 대기 |
| **ANSWERED** | 답변 완료 | 파트너가 답변 완료 |
| **CANCELLED** | 취소됨 | 고객이 상담 취소 |

---

## 🔍 인덱스 (빠른 검색용)

| 인덱스명 | 대상 컬럼 | 목적 |
|---------|----------|------|
| idx_user_id | user_id | 특정 고객의 상담 목록 조회 |
| idx_partner_id | partner_id | 특정 파트너가 받은 상담 목록 조회 |
| idx_status | status | 상태별 필터링 (답변 대기중만 보기) |
| idx_created_at | created_at | 최신순 정렬 |

---

## 💡 실제 데이터 예시

### 예시 1: 사진 포함 상담 요청

```sql
INSERT INTO consultations VALUES (
  1,                          -- id
  5,                          -- user_id (고객 홍길동)
  3,                          -- partner_id (서울동물병원)
  NULL,                       -- reservation_id (예약 없음)
  '강아지',                    -- pet_type
  '피부병 문의',               -- subject
  '우리 강아지 피부에 붉은 반점이 생겼는데 괜찮을까요?', -- content
  'http://server.com/uploads/consultation_1.jpg',  -- image_url 📷
  NULL,                       -- answer (아직 답변 없음)
  NULL,                       -- answered_at
  'PENDING',                  -- status
  '2025-11-25 14:30:00'      -- created_at
);
```

**화면에 보이는 모습:**
```
[상담 요청]
제목: 피부병 문의
내용: 우리 강아지 피부에 붉은 반점이 생겼는데 괜찮을까요?
📷 [첨부된 이미지 보기]
상태: 답변 대기중
작성일: 2025-11-25 14:30
```

---

### 예시 2: 파트너가 답변한 후

```sql
UPDATE consultations
SET
  answer = '피부 알레르기일 가능성이 있습니다. 한번 내원해주세요.',
  answered_at = '2025-11-25 15:00:00',
  status = 'ANSWERED'
WHERE id = 1;
```

**화면에 보이는 모습:**
```
[상담 요청]
제목: 피부병 문의
내용: 우리 강아지 피부에 붉은 반점이 생겼는데 괜찮을까요?
📷 [첨부된 이미지 보기]

[파트너 답변] ✅
답변: 피부 알레르기일 가능성이 있습니다. 한번 내원해주세요.
답변일: 2025-11-25 15:00
```

---

### 예시 3: 사진 없는 간단한 문의

```sql
INSERT INTO consultations VALUES (
  2,                          -- id
  5,                          -- user_id
  3,                          -- partner_id
  NULL,                       -- reservation_id
  '고양이',                    -- pet_type
  '진료 시간 문의',            -- subject
  '토요일에도 진료하시나요?',   -- content
  NULL,                       -- image_url (사진 없음)
  NULL,                       -- answer
  NULL,                       -- answered_at
  'PENDING',                  -- status
  '2025-11-25 16:00:00'      -- created_at
);
```

---

## 🔗 다른 테이블과의 관계

```
users (고객)
  ↓ (user_id)
consultations ← 상담 요청/응답
  ↓ (partner_id)
partner (병원/펫시터)

reservation (예약) → (선택적 연결)
```

---

## 📤 API 사용 예시

### 1. 이미지 포함 상담 생성

```bash
POST http://localhost:9075/api/v1/consultations
Content-Type: application/json

{
  "userId": 5,
  "partnerId": 3,
  "petType": "강아지",
  "subject": "피부병 문의",
  "content": "우리 강아지 피부에 붉은 반점이 생겼어요",
  "imageUrl": "http://server.com/uploads/consultation_1.jpg"
}
```

### 2. 이미지 없는 상담 생성

```bash
POST http://localhost:9075/api/v1/consultations
Content-Type: application/json

{
  "userId": 5,
  "partnerId": 3,
  "petType": "고양이",
  "subject": "진료 시간 문의",
  "content": "토요일에도 진료하시나요?",
  "imageUrl": null
}
```

---

## ✅ 구현 완료 사항

### 백엔드
- ✅ SQL 테이블 스키마 (image_url 컬럼 포함)
- ✅ Consultation.java Entity (imageUrl 필드)
- ✅ ConsultationCreateReq DTO (imageUrl 필드)
- ✅ ConsultationRes DTO (imageUrl 필드)
- ✅ ConsultationService (imageUrl 매핑)
- ✅ ConsultationController (API 엔드포인트)

### 프론트엔드
- ✅ ConsultationRecord 모델 (imageUrl 필드)
- ✅ ConsultationService API 연동
- ⏳ SimpleConsultationPage에 이미지 업로드 UI 추가 필요

---

## 🎯 다음 단계

1. **데이터베이스 테이블 생성**
   - [create_consultation_table.sql](backend/demo/src/main/resources/create_consultation_table.sql) 실행

2. **이미지 업로드 기능 구현 (프론트엔드)**
   - 이미지 선택 버튼 추가
   - 선택한 이미지를 서버에 업로드
   - 업로드된 이미지 URL을 받아서 상담 생성 시 전달

3. **테스트**
   - 이미지 포함 상담 요청
   - 이미지 없는 상담 요청
   - 상담 목록에서 이미지 표시

---

## 💾 테이블 생성 SQL

```sql
CREATE TABLE IF NOT EXISTS consultations (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '상담 ID',
    user_id BIGINT NOT NULL COMMENT '고객 ID (users 테이블 FK)',
    partner_id BIGINT NOT NULL COMMENT '파트너 ID (partner 테이블 FK)',
    reservation_id BIGINT NULL COMMENT '연결된 예약 ID (선택적)',
    pet_type VARCHAR(20) NOT NULL COMMENT '반려동물 종류 (강아지, 고양이 등)',
    subject VARCHAR(100) NOT NULL COMMENT '상담 제목',
    content TEXT NOT NULL COMMENT '상담 내용',
    image_url VARCHAR(500) NULL COMMENT '첨부 이미지 URL (선택적)',
    answer TEXT NULL COMMENT '파트너 답변',
    answered_at DATETIME NULL COMMENT '답변 일시',
    status VARCHAR(20) NOT NULL DEFAULT 'PENDING' COMMENT '상담 상태',
    created_at DATETIME NOT NULL COMMENT '생성 일시',

    INDEX idx_user_id (user_id),
    INDEX idx_partner_id (partner_id),
    INDEX idx_status (status),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 🎨 주요 변경사항 요약

### ✨ 추가된 기능: 이미지 첨부

**데이터베이스:**
- `image_url` VARCHAR(500) 컬럼 추가

**백엔드 (Java):**
- Consultation Entity에 `imageUrl` 필드 추가
- ConsultationCreateReq에 `imageUrl` 추가
- ConsultationRes에 `imageUrl` 추가
- ConsultationService에서 imageUrl 처리

**프론트엔드 (Dart):**
- ConsultationRecord에 `imageUrl` 필드 추가
- JSON 파싱 시 imageUrl 처리

**장점:**
- 사진으로 증상을 명확하게 전달 가능
- 파트너가 더 정확한 답변 제공 가능
- 선택적 기능 (사진 없이도 상담 가능)
