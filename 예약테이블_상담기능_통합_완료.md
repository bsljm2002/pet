# 예약(Reservation) 테이블 기반 상담 기능 구현 완료 ✅

## 🎯 구현 방식

기존 `consultations` 테이블을 사용하지 않고, **`reservation` 테이블에 상담 컬럼을 추가**하여 예약과 상담을 통합했습니다.

---

## ✨ 주요 특징

### 1. **간단한 구조**
- 별도 테이블 불필요
- 예약과 상담이 1:1 관계로 자연스럽게 연결
- 데이터 중복 없음

### 2. **완료된 예약에만 상담 가능**
- 예약 상태가 `COMPLETED`일 때만 상담 요청 가능
- "추가 상담" 버튼으로 간편하게 접근

### 3. **상담 상태 관리**
- `NONE`: 상담 요청 안 함 (기본값)
- `PENDING`: 답변 대기 중
- `ANSWERED`: 답변 완료

---

## 📝 수정된 파일

### Backend

#### 1. **add_consultation_to_reservation.sql** (새 파일)
데이터베이스 마이그레이션 스크립트:
```sql
ALTER TABLE reservation
ADD COLUMN consultation_subject VARCHAR(100) NULL COMMENT '상담 제목',
ADD COLUMN consultation_content TEXT NULL COMMENT '상담 내용',
ADD COLUMN consultation_answer TEXT NULL COMMENT '파트너 답변',
ADD COLUMN consultation_status VARCHAR(20) DEFAULT 'NONE' COMMENT '상담 상태: NONE/PENDING/ANSWERED',
ADD COLUMN consultation_requested_at DATETIME NULL COMMENT '상담 요청 일시',
ADD COLUMN consultation_answered_at DATETIME NULL COMMENT '답변 일시';

CREATE INDEX idx_consultation_status ON reservation(consultation_status);
```

#### 2. **Reservation.java** (엔티티)
상담 관련 필드 추가:
```java
// 상담 관련 필드
@Column(name = "consultation_subject", length = 100)
private String consultationSubject;

@Column(name = "consultation_content", columnDefinition = "TEXT")
private String consultationContent;

@Column(name = "consultation_answer", columnDefinition = "TEXT")
private String consultationAnswer;

@Enumerated(EnumType.STRING)
@Column(name = "consultation_status", length = 20)
@Builder.Default
private ConsultationStatus consultationStatus = ConsultationStatus.NONE;

@Column(name = "consultation_requested_at")
private LocalDateTime consultationRequestedAt;

@Column(name = "consultation_answered_at")
private LocalDateTime consultationAnsweredAt;

// Enum 추가
public enum ConsultationStatus {
    NONE,      // 상담 요청 안 함
    PENDING,   // 상담 대기 중
    ANSWERED   // 답변 완료
}

// 비즈니스 메서드
public void requestConsultation(String subject, String content) {
    this.consultationSubject = subject;
    this.consultationContent = content;
    this.consultationStatus = ConsultationStatus.PENDING;
    this.consultationRequestedAt = LocalDateTime.now();
}

public void answerConsultation(String answer) {
    this.consultationAnswer = answer;
    this.consultationStatus = ConsultationStatus.ANSWERED;
    this.consultationAnsweredAt = LocalDateTime.now();
}
```

#### 3. **MyReservationRes.java** (DTO)
상담 정보 포함:
```java
public record MyReservationRes(
    // ... 기존 필드들
    String consultationStatus,
    String consultationSubject,
    String consultationContent,
    String consultationAnswer,
    String consultationRequestedAt,
    String consultationAnsweredAt
) {}
```

#### 4. **ConsultationRequestDto.java** (새 파일)
```java
public record ConsultationRequestDto(
    String subject,
    String content
) {}
```

#### 5. **ReservationService.java**
새 메서드 추가:
```java
/**
 * 예약에 대한 상담 요청 (사용자)
 */
public void requestConsultation(Long reservationId, Long userId, String subject, String content) {
    Reservation reservation = get(reservationId);

    // 사용자 검증
    if (!reservation.getUserId().equals(userId)) {
        throw new IllegalArgumentException("USER_NOT_MATCHED");
    }

    // 완료된 예약에만 상담 가능
    if (reservation.getStatus() != ReservationStatus.COMPLETED) {
        throw new IllegalArgumentException("CONSULTATION_ONLY_FOR_COMPLETED");
    }

    reservation.requestConsultation(subject, content);
    reservations.save(reservation);

    // 파트너에게 알림 전송
    // ...
}

/**
 * 상담에 답변 (파트너)
 */
public void answerConsultation(Long reservationId, Long partnerId, String answer) {
    // 파트너 검증 후
    reservation.answerConsultation(answer);
    reservations.save(reservation);

    // 사용자에게 알림 전송
    // ...
}
```

`toRes` 메서드 수정: 상담 정보 포함

#### 6. **ReservationController.java**
새 API 엔드포인트:
```java
/**
 * 예약에 대한 상담 요청 (사용자)
 * POST /api/v1/reservations/{id}/consultation
 */
@PostMapping("/{id}/consultation")
public ResponseEntity<ApiResponse<Map<String, String>>> requestConsultation(
    @PathVariable("id") Long reservationId,
    @RequestParam("userId") Long userId,
    @RequestParam("subject") String subject,
    @RequestParam("content") String content
) {
    reservationService.requestConsultation(reservationId, userId, subject, content);
    return ResponseEntity.ok(ApiResponse.ok(Map.of("message", "상담 요청이 완료되었습니다")));
}

/**
 * 상담에 답변 (파트너)
 * PATCH /api/v1/reservations/{id}/consultation/answer
 */
@PatchMapping("/{id}/consultation/answer")
public ResponseEntity<ApiResponse<Map<String, String>>> answerConsultation(
    @PathVariable("id") Long reservationId,
    @RequestParam("partnerId") Long partnerId,
    @RequestParam("answer") String answer
) {
    reservationService.answerConsultation(reservationId, partnerId, answer);
    return ResponseEntity.ok(ApiResponse.ok(Map.of("message", "답변이 완료되었습니다")));
}
```

### Frontend

#### 1. **user_reservation_model.dart** (모델)
상담 관련 필드 추가:
```dart
class UserReservationModel {
  // ... 기존 필드들

  // 상담 관련 필드
  final String consultationStatus; // NONE, PENDING, ANSWERED
  final String? consultationSubject;
  final String? consultationContent;
  final String? consultationAnswer;
  final String? consultationRequestedAt;
  final String? consultationAnsweredAt;

  // Getter 추가
  String get consultationStatusLabel {
    switch (consultationStatus) {
      case 'NONE':
        return '상담 없음';
      case 'PENDING':
        return '답변 대기중';
      case 'ANSWERED':
        return '답변 완료';
      default:
        return consultationStatus;
    }
  }
}
```

#### 2. **user_reservation_service.dart**
새 메서드 추가:
```dart
/// 예약에 대한 상담 요청
Future<bool> requestConsultation({
  required int reservationId,
  required int userId,
  required String subject,
  required String content,
}) async {
  final url = Uri.parse(
    '${ApiService.baseUrl}/reservations/$reservationId/consultation?userId=$userId&subject=${Uri.encodeComponent(subject)}&content=${Uri.encodeComponent(content)}',
  );

  final response = await http.post(url, headers: {'Content-Type': 'application/json'});

  if (response.statusCode == 200) {
    final jsonData = json.decode(utf8.decode(response.bodyBytes));
    return jsonData['ok'] == true;
  }

  return false;
}
```

#### 3. **reservation_consultation_page.dart** (새 파일)
예약에 대한 상담 요청 전용 페이지:
- 예약 정보 표시 (파트너명, 서비스 타입, 날짜, 시간)
- 상담 제목 입력
- 상담 내용 입력 (여러 줄)
- 제출 버튼

#### 4. **my_reservation.dart**
- 사용하지 않는 import 제거 (VetModel, SitterModel)
- `_ReservationTicket`에 `userId` 필드 추가
- 간소화된 `_requestConsultation` 메서드:
  ```dart
  void _requestConsultation(BuildContext context, UserReservationModel reservation) async {
    final result = await Navigator.push(
      context,
      MaterialPageRoute(
        builder: (context) => ReservationConsultationPage(
          reservation: reservation,
          userId: userId,
        ),
      ),
    );

    if (result == true) {
      onRefresh();
    }
  }
  ```

---

## 🗄️ 데이터베이스 마이그레이션 (필수)

### 실행 방법:

#### MySQL Workbench / DBeaver 사용:
1. 데이터베이스에 연결
2. 아래 SQL 실행:

```sql
-- 1. 컬럼 추가
ALTER TABLE reservation
ADD COLUMN consultation_subject VARCHAR(100) NULL COMMENT '상담 제목',
ADD COLUMN consultation_content TEXT NULL COMMENT '상담 내용',
ADD COLUMN consultation_answer TEXT NULL COMMENT '파트너 답변',
ADD COLUMN consultation_status VARCHAR(20) DEFAULT 'NONE' COMMENT '상담 상태',
ADD COLUMN consultation_requested_at DATETIME NULL COMMENT '상담 요청 일시',
ADD COLUMN consultation_answered_at DATETIME NULL COMMENT '답변 일시';

-- 2. 인덱스 추가
CREATE INDEX idx_consultation_status ON reservation(consultation_status);

-- 3. 확인
SHOW COLUMNS FROM reservation LIKE 'consultation%';
```

---

## 🚀 사용 시나리오

### 시나리오: 예약 완료 후 추가 상담

1. 사용자가 병원 예약을 하고 진료를 받음
2. 파트너가 예약 상태를 `COMPLETED`로 변경
3. **사용자가 "내 예약" 페이지에서 완료된 예약 확인**
4. **"이 예약에 대해 추가 상담하기" 버튼 클릭**
5. 상담 요청 페이지로 이동 (예약 정보가 자동으로 표시됨)
6. 추가 질문 작성 (예: "처방받은 약 복용 후 이상 증상이 있어요")
7. 제출 → `consultation_status`가 `PENDING`으로 변경
8. 파트너가 답변 작성 → `consultation_status`가 `ANSWERED`로 변경
9. 사용자가 "내 예약" 페이지에서 답변 확인

---

## 📊 API 엔드포인트

### 1. 상담 요청 (사용자)
```
POST /api/v1/reservations/{id}/consultation
Query Parameters:
  - userId: Long
  - subject: String
  - content: String

Response: { "ok": true, "data": { "message": "상담 요청이 완료되었습니다" } }
```

### 2. 상담 답변 (파트너)
```
PATCH /api/v1/reservations/{id}/consultation/answer
Query Parameters:
  - partnerId: Long
  - answer: String

Response: { "ok": true, "data": { "message": "답변이 완료되었습니다" } }
```

### 3. 내 예약 조회 (상담 정보 포함)
```
GET /api/v1/reservations/mine?userId={userId}

Response: {
  "ok": true,
  "data": [
    {
      "reservationId": 123,
      "userId": 1,
      ...
      "consultationStatus": "ANSWERED",
      "consultationSubject": "약 복용 관련 문의",
      "consultationContent": "처방받은 약을 먹었는데 졸려요",
      "consultationAnswer": "그 약의 부작용이 맞습니다. 복용 시간을 저녁으로 조정해보세요.",
      "consultationRequestedAt": "2025.11.25 14:30",
      "consultationAnsweredAt": "2025.11.25 16:20"
    }
  ]
}
```

---

## 🔧 테스트 방법

### 1. 데이터베이스 마이그레이션 실행 ⚠️
위의 SQL 스크립트 실행

### 2. 백엔드 서버 재시작
```bash
cd backend/demo
./gradlew bootRun
```

### 3. 프론트엔드 실행
```bash
cd front/main
flutter run
```

### 4. 기능 테스트 체크리스트
- [ ] 데이터베이스에 `consultation_*` 컬럼들이 추가됨
- [ ] 백엔드 서버 정상 시작됨
- [ ] 프론트엔드 앱 정상 실행됨
- [ ] 예약을 하나 생성하고 상태를 `COMPLETED`로 변경
- [ ] "내 예약" 페이지에서 완료된 예약 확인
- [ ] "이 예약에 대해 추가 상담하기" 버튼 표시됨
- [ ] 버튼 클릭 시 상담 페이지로 이동됨
- [ ] 예약 정보가 자동으로 표시됨
- [ ] 상담 제목과 내용 입력 후 제출 성공
- [ ] "내 예약" 목록에서 상담 상태가 "답변 대기중"으로 표시됨
- [ ] 파트너가 답변 작성 (API 또는 파트너 앱)
- [ ] "내 예약"에서 답변 완료 확인

---

## ✅ 장점

1. **간단한 구조**: 별도 테이블 불필요, 예약과 자연스럽게 통합
2. **데이터 일관성**: 예약과 상담이 1:1로 연결되어 데이터 무결성 유지
3. **쉬운 조회**: 예약 조회 시 상담 정보도 함께 반환
4. **맥락 유지**: 어떤 예약에 대한 상담인지 명확함

---

## 📋 제한사항

1. **한 예약당 상담 1개만 가능**: 여러 번 질문하려면 매번 새로운 내용으로 덮어씀
   - 필요시 상담 이력을 별도로 관리하거나, 여러 번 상담하려면 `consultations` 테이블 방식 사용
2. **예약 없는 간편 상담 불가**: 예약을 완료해야만 상담 가능
   - 예약 전 문의는 다른 방식으로 처리 필요

---

## 🔄 향후 개선 아이디어

1. **상담 이력 관리**: 한 예약에 대해 여러 번 상담 가능하도록 개선
2. **예약 상세 페이지에 상담 탭 추가**: 예약 정보와 상담을 한 곳에서 확인
3. **알림 개선**: 상담 답변 시 푸시 알림 강화
4. **파일 첨부**: 상담 시 사진/파일 첨부 가능하도록

---

**구현 완료일:** 2025-11-25
**구현 방식:** Reservation 테이블에 상담 컬럼 추가 (consultations 테이블 미사용)

**핵심 변경:** 예약과 상담을 하나의 테이블로 통합하여 간단하고 효율적인 구조 구현! 🎉
