# 의사/시터 프론트엔드-백엔드 연결 가이드

## 📋 목차
1. [현재 상태 파악](#1-현재-상태-파악)
2. [백엔드 API 엔드포인트](#2-백엔드-api-엔드포인트)
3. [프론트엔드 수정 사항](#3-프론트엔드-수정-사항)
4. [단계별 연결 가이드](#4-단계별-연결-가이드)
5. [테스트 방법](#5-테스트-방법)
6. [문제 해결](#6-문제-해결)

---

## 1. 현재 상태 파악

### ✅ 백엔드 구현 상태
백엔드에 **수의사/펫시터 관련 API가 모두 구현**되어 있습니다!

**구현된 API:**
- ✅ 파트너(수의사/펫시터) 목록 조회
- ✅ 예약 생성
- ✅ 내 예약 목록 조회
- ✅ 예약 승인
- ✅ 이미지 업로드

### ⚠️ 프론트엔드 현재 상태
프론트엔드는 현재 **더미 데이터**를 사용하고 있습니다.
- `api_service.dart`의 `baseUrl`이 `'https://your-api-server.com/api'`로 설정되어 있음
- API 호출 실패 시 더미 데이터 반환

---

## 2. 백엔드 API 엔드포인트

### 🏥 수의사 목록 조회
**엔드포인트:** `GET /api/v1/users/partners?user_type=HOSPITAL`

**응답 예시:**
```json
{
  "success": true,
  "data": {
    "user_type": "HOSPITAL",
    "total": 5,
    "result": [
      {
        "id": 1,
        "username": "김루나 수의사",
        "vet_specialty": ["INTERNAL_MEDICINE"],
        "ca_categorical": "BOTH",
        "image_url": "/media/users/1/profile.jpg",
        "working_schedule": {
          "working_days": ["월", "화", "수", "목", "금"],
          "working_start_hours": "09:00",
          "working_end_hours": "18:00"
        }
      }
    ]
  }
}
```

### 🐕 펫시터 목록 조회
**엔드포인트:** `GET /api/v1/users/partners?user_type=SITTER`

**응답 예시:**
```json
{
  "success": true,
  "data": {
    "user_type": "SITTER",
    "total": 3,
    "result": [
      {
        "id": 5,
        "username": "박해리 시터",
        "petsitter": ["WALK"],
        "image_url": "/media/users/5/profile.jpg",
        "working_schedule": {
          "working_days": ["월", "수", "금"],
          "working_start_hours": "10:00",
          "working_end_hours": "17:00"
        }
      }
    ]
  }
}
```

### 📝 예약 생성
**엔드포인트:** `POST /api/v1/reservations`

**요청 본문:**
```json
{
  "userId": 1,
  "partnerId": 2,
  "serviceCategorical": "HOSPITAL",
  "petId": 1,
  "vetSpecialtyCsv": "INTERNAL_MEDICINE,DENTISTRY",
  "reservationContent": "감기 증상 진료 요청"
}
```

**응답 예시:**
```json
{
  "success": true,
  "data": {
    "reservation_id": 10
  }
}
```

### 📋 내 예약 목록 조회
**엔드포인트:** `GET /api/v1/reservations/mine?userId={userId}&serviceType=HOSPITAL`

**응답 예시:**
```json
{
  "success": true,
  "data": [
    {
      "reservationId": 1,
      "partnerId": 2,
      "partnerName": "김루나 수의사",
      "serviceCategorical": "HOSPITAL",
      "status": "WAITING",
      "createdAt": "2025-01-15T10:30:00+09:00",
      "petName": "멍멍이",
      "reservationContent": "감기 증상"
    }
  ]
}
```

### 백엔드 Enum 매핑표

#### 수의사 전문 분야 (VetSpecialty)
| 백엔드 코드 | 한글 |
|------------|------|
| INTERNAL_MEDICINE | 내과 |
| SURGERY | 외과 |
| ORTHOPEDICS | 정형외과 |
| OPHTHALMOLOGY | 안과 |
| DENTISTRY | 치과 |
| DERMATOLOGY | 피부과 |
| EMERGENCY_MEDICINE | 응급의학과 |
| GENERAL | 기타/전체 |

#### 펫시터 업무 (PetsitterWork)
| 백엔드 코드 | 한글 |
|------------|------|
| WALK | 산책 |
| TRANSPORT | 이동 |
| HYGIENE | 위생관리 |
| ALL | 전체 |

#### 예약 상태 (ReservationStatus)
| 백엔드 코드 | 한글 |
|------------|------|
| WAITING | 대기중 |
| CONFIRMED | 승인됨 |
| CHECKED_IN | 체크인 완료 |
| COMPLETED | 완료 |
| CANCELLED_BY_USER | 사용자 취소 |
| CANCELLED_BY_BIZ | 업체 취소 |

---

## 3. 프론트엔드 수정 사항

### 📁 수정할 파일
1. `lib/services/api_service.dart` - API 엔드포인트 연결
2. `lib/models/vet_model.dart` - 백엔드 응답 매핑
3. `lib/models/reservation_model.dart` - 백엔드 요청/응답 매핑

---

## 4. 단계별 연결 가이드

### ✨ Step 1: VetService 파일 생성

먼저 수의사/펫시터 관련 API를 처리할 서비스 파일을 생성합니다.

**파일 생성:** `lib/services/vet_service.dart`

```dart
import 'dart:convert';
import 'package:http/http.dart' as http;

class VetService {
  // Android 에뮬레이터용 baseURL
  static const String baseUrl = 'http://10.0.2.2:9075/api/v1';

  // 이미지 URL을 완전한 URL로 변환
  static String getFullImageUrl(String? imageUrl) {
    if (imageUrl == null || imageUrl.isEmpty) return '';
    if (imageUrl.startsWith('http://') || imageUrl.startsWith('https://')) {
      return imageUrl;
    }
    return 'http://10.0.2.2:9075$imageUrl';
  }

  /// 파트너(수의사/펫시터) 목록 조회
  ///
  /// [userType]: 'HOSPITAL' 또는 'SITTER'
  static Future<Map<String, dynamic>> getPartners(String userType) async {
    try {
      final response = await http.get(
        Uri.parse('$baseUrl/users/partners?user_type=$userType'),
        headers: {'Content-Type': 'application/json'},
      );

      if (response.statusCode == 200) {
        final jsonResponse = json.decode(response.body);
        return {
          'success': jsonResponse['success'] ?? true,
          'data': jsonResponse['data'],
        };
      } else {
        throw Exception('파트너 목록을 불러오는데 실패했습니다');
      }
    } catch (e) {
      print('파트너 조회 오류: $e');
      return {
        'success': false,
        'message': e.toString(),
      };
    }
  }

  /// 예약 생성
  ///
  /// [userId]: 사용자 ID
  /// [partnerId]: 파트너(수의사/시터) ID
  /// [serviceCategorical]: 'HOSPITAL', 'SITTER' 등
  /// [petId]: 펫 ID
  /// [content]: 예약 내용
  /// [vetSpecialty]: 수의사 진료과목 (선택, 쉼표로 구분)
  /// [petsitterWork]: 펫시터 업무 (선택, 쉼표로 구분)
  static Future<Map<String, dynamic>> createReservation({
    required int userId,
    required int partnerId,
    required String serviceCategorical,
    required int petId,
    String? content,
    String? vetSpecialty,
    String? petsitterWork,
  }) async {
    try {
      final Map<String, dynamic> requestBody = {
        'userId': userId,
        'partnerId': partnerId,
        'serviceCategorical': serviceCategorical,
        'petId': petId,
      };

      if (content != null && content.isNotEmpty) {
        requestBody['reservationContent'] = content;
      }
      if (vetSpecialty != null && vetSpecialty.isNotEmpty) {
        requestBody['vetSpecialtyCsv'] = vetSpecialty;
      }
      if (petsitterWork != null && petsitterWork.isNotEmpty) {
        requestBody['petsitterWorkCsv'] = petsitterWork;
      }

      final response = await http.post(
        Uri.parse('$baseUrl/reservations'),
        headers: {'Content-Type': 'application/json'},
        body: json.encode(requestBody),
      );

      if (response.statusCode == 200) {
        final jsonResponse = json.decode(response.body);
        return {
          'success': jsonResponse['success'] ?? true,
          'data': jsonResponse['data'],
        };
      } else {
        throw Exception('예약 생성에 실패했습니다');
      }
    } catch (e) {
      print('예약 생성 오류: $e');
      return {
        'success': false,
        'message': e.toString(),
      };
    }
  }

  /// 내 예약 목록 조회
  ///
  /// [userId]: 사용자 ID
  /// [serviceType]: 'HOSPITAL' 또는 'SITTER' (선택)
  static Future<Map<String, dynamic>> getMyReservations({
    required int userId,
    String? serviceType,
  }) async {
    try {
      String url = '$baseUrl/reservations/mine?userId=$userId';
      if (serviceType != null && serviceType.isNotEmpty) {
        url += '&serviceType=$serviceType';
      }

      final response = await http.get(
        Uri.parse(url),
        headers: {'Content-Type': 'application/json'},
      );

      if (response.statusCode == 200) {
        final jsonResponse = json.decode(response.body);
        return {
          'success': jsonResponse['success'] ?? true,
          'data': jsonResponse['data'],
        };
      } else {
        throw Exception('예약 목록을 불러오는데 실패했습니다');
      }
    } catch (e) {
      print('예약 목록 조회 오류: $e');
      return {
        'success': false,
        'message': e.toString(),
      };
    }
  }
}
```

---

### ✨ Step 2: HospitalProvider 수정

**파일:** `lib/providers/hospital_provider.dart`

기존의 `loadVets()` 메서드를 다음과 같이 수정합니다:

```dart
import '../services/vet_service.dart';

// ... 기존 코드 ...

/// 수의사 목록 로드
Future<void> loadVets() async {
  _isLoadingVets = true;
  _vetsError = null;
  notifyListeners();

  try {
    // 백엔드 API 호출
    final response = await VetService.getPartners('HOSPITAL');

    if (response['success'] == true) {
      final data = response['data'];
      final List<dynamic> result = data['result'] ?? [];

      // 백엔드 응답을 VetModel로 변환
      _vets = result.map((partnerJson) {
        return VetModel(
          id: partnerJson['id'].toString(),
          name: partnerJson['username'] ?? '',
          doctorName: partnerJson['username'] ?? '',
          address: '주소 정보 없음', // 백엔드에 주소 필드 추가 필요
          phone: '전화번호 없음', // 백엔드에 전화번호 필드 추가 필요
          rating: 4.5, // 백엔드에 평점 필드 추가 필요
          specialties: _parseSpecialties(partnerJson['vet_specialty']),
          availableTimes: _parseWorkingHours(partnerJson['working_schedule']),
          distance: 0.0, // 거리 계산 로직 추가 필요
          isOpen: true, // 영업시간 체크 로직 추가 필요
          imageUrl: VetService.getFullImageUrl(partnerJson['image_url']),
          description: '수의사 소개', // 백엔드에 소개 필드 추가 필요
        );
      }).toList();
    } else {
      _vetsError = response['message'] ?? '수의사 목록을 불러올 수 없습니다';
      _vets = [];
    }
  } catch (e) {
    _vetsError = e.toString();
    _vets = [];
  } finally {
    _isLoadingVets = false;
    notifyListeners();
  }
}

// 진료과목 파싱 (백엔드 enum을 한글로 변환)
List<String> _parseSpecialties(dynamic specialties) {
  if (specialties == null) return [];

  final Map<String, String> specialtyMap = {
    'INTERNAL_MEDICINE': '내과',
    'SURGERY': '외과',
    'ORTHOPEDICS': '정형외과',
    'OPHTHALMOLOGY': '안과',
    'DENTISTRY': '치과',
    'DERMATOLOGY': '피부과',
    'EMERGENCY_MEDICINE': '응급의학과',
    'GENERAL': '일반',
  };

  if (specialties is List) {
    return specialties
        .map((s) => specialtyMap[s] ?? s.toString())
        .toList();
  }
  return [];
}

// 근무시간 파싱
List<String> _parseWorkingHours(dynamic schedule) {
  if (schedule == null) return [];

  try {
    String start = schedule['working_start_hours'] ?? '09:00';
    String end = schedule['working_end_hours'] ?? '18:00';

    // 시간 슬롯 생성 (예: 09:00, 10:00, 11:00, ...)
    List<String> times = [];
    int startHour = int.parse(start.split(':')[0]);
    int endHour = int.parse(end.split(':')[0]);

    for (int hour = startHour; hour < endHour; hour++) {
      times.add('${hour.toString().padLeft(2, '0')}:00');
    }

    return times;
  } catch (e) {
    return ['09:00', '10:00', '11:00', '14:00', '15:00', '16:00'];
  }
}
```

---

### ✨ Step 3: 예약 생성 기능 수정

**파일:** `lib/providers/hospital_provider.dart`

예약 생성 메서드를 수정합니다:

```dart
import '../services/auth_service.dart';

// ... 기존 코드 ...

/// 예약 생성
Future<bool> createReservation({
  required int partnerId,
  required int petId,
  required String serviceCategorical,
  String? content,
  List<String>? specialties,
}) async {
  try {
    // 현재 로그인한 사용자 정보 가져오기
    final currentUser = AuthService.getCurrentUser();
    if (currentUser == null) {
      _reservationsError = '로그인이 필요합니다';
      return false;
    }

    // 진료과목을 백엔드 형식으로 변환 (한글 -> 영문 enum)
    String? vetSpecialtyCsv;
    if (specialties != null && specialties.isNotEmpty) {
      final Map<String, String> reverseMap = {
        '내과': 'INTERNAL_MEDICINE',
        '외과': 'SURGERY',
        '정형외과': 'ORTHOPEDICS',
        '안과': 'OPHTHALMOLOGY',
        '치과': 'DENTISTRY',
        '피부과': 'DERMATOLOGY',
        '응급의학과': 'EMERGENCY_MEDICINE',
        '일반': 'GENERAL',
      };

      List<String> enumValues = specialties
          .map((s) => reverseMap[s] ?? 'GENERAL')
          .toList();
      vetSpecialtyCsv = enumValues.join(',');
    }

    // 백엔드 API 호출
    final response = await VetService.createReservation(
      userId: currentUser.id,
      partnerId: partnerId,
      serviceCategorical: serviceCategorical,
      petId: petId,
      content: content,
      vetSpecialty: vetSpecialtyCsv,
    );

    if (response['success'] == true) {
      // 예약 성공 시 목록 새로고침
      await loadReservations();
      return true;
    } else {
      _reservationsError = response['message'] ?? '예약 생성에 실패했습니다';
      return false;
    }
  } catch (e) {
    _reservationsError = e.toString();
    return false;
  }
}
```

---

### ✨ Step 4: 예약 목록 조회 수정

**파일:** `lib/providers/hospital_provider.dart`

```dart
/// 예약 목록 로드
Future<void> loadReservations() async {
  _isLoadingReservations = true;
  _reservationsError = null;
  notifyListeners();

  try {
    // 현재 로그인한 사용자 정보 가져오기
    final currentUser = AuthService.getCurrentUser();
    if (currentUser == null) {
      _reservationsError = '로그인이 필요합니다';
      _reservations = [];
      _isLoadingReservations = false;
      notifyListeners();
      return;
    }

    // 백엔드 API 호출
    final response = await VetService.getMyReservations(
      userId: currentUser.id,
      serviceType: 'HOSPITAL', // 또는 'SITTER'
    );

    if (response['success'] == true) {
      final List<dynamic> data = response['data'] ?? [];

      // 백엔드 응답을 ReservationModel로 변환
      _reservations = data.map((json) {
        return ReservationModel(
          id: json['reservationId'].toString(),
          vetId: json['partnerId'].toString(),
          vetName: json['partnerName'] ?? '',
          petName: json['petName'] ?? '',
          petType: json['petType'] ?? '강아지',
          specialty: json['serviceCategorical'] ?? '',
          reservationDate: DateTime.parse(json['createdAt']),
          timeSlot: '시간 미정', // 백엔드에 시간 슬롯 추가 필요
          status: _parseStatus(json['status']),
          memo: json['reservationContent'] ?? '',
        );
      }).toList();
    } else {
      _reservationsError = response['message'] ?? '예약 목록을 불러올 수 없습니다';
      _reservations = [];
    }
  } catch (e) {
    _reservationsError = e.toString();
    _reservations = [];
  } finally {
    _isLoadingReservations = false;
    notifyListeners();
  }
}

// 예약 상태 파싱 (백엔드 enum을 한글로 변환)
String _parseStatus(String? status) {
  final Map<String, String> statusMap = {
    'WAITING': '대기중',
    'CONFIRMED': '승인됨',
    'CHECKED_IN': '체크인 완료',
    'COMPLETED': '완료',
    'CANCELLED_BY_USER': '사용자 취소',
    'CANCELLED_BY_BIZ': '업체 취소',
  };

  return statusMap[status] ?? '알 수 없음';
}
```

---

### ✨ Step 5: 펫시터 탭도 동일하게 수정

`hospital_screen.dart`에서 펫시터 탭도 동일한 방식으로 수정합니다.

펫시터용 Provider를 만들거나, HospitalProvider에 `loadSitters()` 메서드를 추가합니다:

```dart
/// 펫시터 목록 로드
Future<void> loadSitters() async {
  _isLoadingVets = true;
  _vetsError = null;
  notifyListeners();

  try {
    final response = await VetService.getPartners('SITTER');

    if (response['success'] == true) {
      final data = response['data'];
      final List<dynamic> result = data['result'] ?? [];

      // 백엔드 응답을 VetModel로 변환 (펫시터도 동일한 모델 사용)
      _vets = result.map((partnerJson) {
        return VetModel(
          id: partnerJson['id'].toString(),
          name: partnerJson['username'] ?? '',
          doctorName: partnerJson['username'] ?? '',
          address: '주소 정보 없음',
          phone: '전화번호 없음',
          rating: 4.5,
          specialties: _parsePetsitterWork(partnerJson['petsitter']),
          availableTimes: _parseWorkingHours(partnerJson['working_schedule']),
          distance: 0.0,
          isOpen: true,
          imageUrl: VetService.getFullImageUrl(partnerJson['image_url']),
          description: '펫시터 소개',
        );
      }).toList();
    } else {
      _vetsError = response['message'] ?? '펫시터 목록을 불러올 수 없습니다';
      _vets = [];
    }
  } catch (e) {
    _vetsError = e.toString();
    _vets = [];
  } finally {
    _isLoadingVets = false;
    notifyListeners();
  }
}

// 펫시터 업무 파싱
List<String> _parsePetsitterWork(dynamic works) {
  if (works == null) return [];

  final Map<String, String> workMap = {
    'WALK': '산책',
    'TRANSPORT': '이동',
    'HYGIENE': '위생관리',
    'ALL': '전체',
  };

  if (works is List) {
    return works.map((w) => workMap[w] ?? w.toString()).toList();
  }
  return [];
}
```

---

## 5. 테스트 방법

### 🧪 Step 1: 백엔드 서버 시작

```bash
cd C:\project\backend\demo
./gradlew bootRun
```

서버가 `http://localhost:9075`에서 실행되는지 확인합니다.

---

### 🧪 Step 2: 테스트용 수의사 데이터 확인

브라우저나 Postman에서 다음 URL을 호출해봅니다:

```
http://localhost:9075/api/v1/users/partners?user_type=HOSPITAL
```

**예상 응답:**
```json
{
  "success": true,
  "data": {
    "user_type": "HOSPITAL",
    "total": 1,
    "result": [...]
  }
}
```

데이터가 없다면 먼저 수의사 사용자를 생성해야 합니다.

---

### 🧪 Step 3: Flutter 앱 실행

```bash
cd C:\project\front\main
flutter run
```

---

### 🧪 Step 4: 기능 테스트

1. **수의사 목록 확인**
   - 의사/시터 탭 클릭
   - 수의사 찾기 탭에서 목록이 로드되는지 확인
   - 로딩 인디케이터가 표시되는지 확인

2. **예약 생성 테스트**
   - 수의사 선택
   - 펫 선택
   - 예약 내용 입력
   - 예약 생성 버튼 클릭
   - 성공 메시지 확인

3. **내 예약 목록 확인**
   - "내 예약" 버튼 클릭
   - 방금 생성한 예약이 목록에 표시되는지 확인

---

### 🧪 Step 5: 디버그 로그 확인

Flutter 콘솔에서 다음과 같은 로그를 확인합니다:

```
I/flutter: 파트너 조회 성공
I/flutter: 총 5명의 수의사
I/flutter: 예약 생성 성공
I/flutter: Reservation ID: 10
```

오류가 발생하면 에러 메시지를 확인합니다:

```
E/flutter: 파트너 조회 오류: SocketException: Failed to connect
E/flutter: 예약 생성 오류: FormatException: Invalid JSON
```

---

## 6. 문제 해결

### ❌ 문제 1: "SocketException: Failed to connect"

**원인:** 백엔드 서버가 실행되지 않았거나, URL이 잘못됨

**해결:**
1. 백엔드 서버가 실행 중인지 확인: `./gradlew bootRun`
2. Android 에뮬레이터에서는 `10.0.2.2`를 사용
3. 실제 기기에서는 PC의 IP 주소 사용 (예: `192.168.0.10`)

---

### ❌ 문제 2: "FormatException: Invalid JSON"

**원인:** 백엔드 응답 형식이 예상과 다름

**해결:**
1. 브라우저에서 API를 직접 호출해서 응답 확인
2. `print(response.body)`로 실제 응답 내용 출력
3. JSON 파싱 코드 수정

---

### ❌ 문제 3: 이미지가 표시되지 않음

**원인:** 이미지 URL이 상대 경로임

**해결:**
`VetService.getFullImageUrl()` 함수가 제대로 호출되고 있는지 확인:

```dart
imageUrl: VetService.getFullImageUrl(partnerJson['image_url']),
```

---

### ❌ 문제 4: "로그인이 필요합니다" 오류

**원인:** `AuthService.getCurrentUser()`가 null을 반환

**해결:**
1. 앱에서 먼저 로그인 수행
2. `AuthService`에서 사용자 정보가 제대로 저장되는지 확인
3. 필요하다면 하드코딩된 userId로 테스트:

```dart
// 테스트용 임시 코드
final userId = 1; // 실제 사용자 ID로 변경
```

---

### ❌ 문제 5: 빈 목록이 표시됨

**원인:** 데이터베이스에 수의사 데이터가 없음

**해결:**

**방법 1: Postman으로 수의사 사용자 생성**

```
POST http://localhost:9075/api/v1/users
Content-Type: application/json

{
  "username": "김루나",
  "nickname": "루나 수의사",
  "email": "luna@example.com",
  "password": "password123",
  "gender": "FEMALE",
  "birthdate": "1985-03-15",
  "address": "서울시 강남구",
  "userType": "HOSPITAL",
  "vetSpecialty": "INTERNAL_MEDICINE",
  "caCategorical": "BOTH",
  "workingDays": "월,화,수,목,금",
  "workingStartHours": "09:00",
  "workingEndHours": "18:00"
}
```

**방법 2: 직접 데이터베이스에 INSERT**

MySQL에 접속해서 users 테이블에 데이터를 직접 삽입합니다.

---

## 🎉 완료 체크리스트

연결 작업이 완료되었는지 확인하세요:

- [ ] `lib/services/vet_service.dart` 파일 생성
- [ ] `lib/providers/hospital_provider.dart`의 `loadVets()` 수정
- [ ] `lib/providers/hospital_provider.dart`의 `createReservation()` 수정
- [ ] `lib/providers/hospital_provider.dart`의 `loadReservations()` 수정
- [ ] 백엔드 서버 실행 확인
- [ ] 수의사 목록 조회 테스트 성공
- [ ] 예약 생성 테스트 성공
- [ ] 내 예약 목록 조회 테스트 성공
- [ ] 이미지가 제대로 표시됨
- [ ] 에러 핸들링이 정상 작동

---

## 📚 추가 개선 사항

백엔드와 프론트엔드가 연결된 후, 다음 기능들을 추가로 개선할 수 있습니다:

### 1. 거리 계산 기능
- 사용자의 현재 위치와 병원 위치 간의 거리 계산
- Google Maps API 또는 위치 기반 서비스 활용

### 2. 영업시간 체크
- 현재 시간과 근무 시간을 비교하여 `isOpen` 값 동적 계산

### 3. 평점 시스템
- 백엔드에 리뷰/평점 테이블 추가
- 사용자가 예약 후 평점을 남길 수 있는 기능

### 4. 실시간 예약 상태 업데이트
- WebSocket 또는 FCM을 통한 푸시 알림
- 예약 승인/거절 시 즉시 알림

### 5. 검색 및 필터링
- 진료 과목, 거리, 평점 등으로 필터링
- 병원명, 수의사명으로 검색

---

## 🔗 관련 파일 참조

- 백엔드 API 컨트롤러: `backend/demo/src/main/java/com/example/pet/demo/users/api/UserController.java`
- 백엔드 예약 컨트롤러: `backend/demo/src/main/java/com/example/pet/demo/reservation/api/ReservationController.java`
- 프론트엔드 화면: `front/main/lib/screens/hospital_screen.dart`
- 프론트엔드 Provider: `front/main/lib/providers/hospital_provider.dart`
- 프론트엔드 모델: `front/main/lib/models/vet_model.dart`

---

**이 가이드를 따라하면 의사/시터 기능이 백엔드와 완전히 연결됩니다!** 🚀

문제가 발생하면 콘솔 로그를 확인하고, 백엔드 응답을 직접 확인하세요.
