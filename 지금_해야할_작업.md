# 🚀 지금 해야 할 작업 목록

## 1️⃣ 데이터베이스 마이그레이션 실행 (필수!) ⚠️

### 실행 방법 A: MySQL Workbench 또는 DBeaver 사용 (추천)

1. **데이터베이스 연결**
   - Host: `project-db-campus.smhrd.com`
   - Port: `3312`
   - Database: `Insa6_aiservice_p3_2`
   - Username: `Insa6_aiservice_p3_2`
   - Password: `aischool2`

2. **SQL 실행** (아래 SQL을 복사해서 실행)
   ```sql
   -- 1. 컬럼 추가
   ALTER TABLE reservation
   ADD COLUMN consultation_subject VARCHAR(100) NULL COMMENT '상담 제목',
   ADD COLUMN consultation_content TEXT NULL COMMENT '상담 내용',
   ADD COLUMN consultation_answer TEXT NULL COMMENT '파트너 답변',
   ADD COLUMN consultation_status VARCHAR(20) DEFAULT 'NONE' COMMENT '상담 상태',
   ADD COLUMN consultation_requested_at DATETIME NULL COMMENT '상담 요청 일시',
   ADD COLUMN consultation_answered_at DATETIME NULL COMMENT '답변 일시';

   -- 2. 인덱스 추가
   CREATE INDEX idx_consultation_status ON reservation(consultation_status);

   -- 3. 확인
   SHOW COLUMNS FROM reservation LIKE 'consultation%';
   ```

3. **결과 확인**
   - `consultation_subject`, `consultation_content`, `consultation_answer`, `consultation_status`, `consultation_requested_at`, `consultation_answered_at` 컬럼이 보여야 함

### 실행 방법 B: MySQL CLI 사용

```bash
# Windows에서 실행 (Git Bash 또는 PowerShell)
mysql -u Insa6_aiservice_p3_2 -p -h project-db-campus.smhrd.com -P 3312 Insa6_aiservice_p3_2 < backend/demo/src/main/resources/add_consultation_to_reservation.sql
```

패스워드 입력: `aischool2`

---

## 2️⃣ 백엔드 서버 재시작

데이터베이스 마이그레이션 후 백엔드를 **반드시** 재시작해야 합니다.

### 방법 A: Gradle 사용
```bash
cd backend/demo
./gradlew bootRun
```

### 방법 B: IDE에서 재시작
- IntelliJ IDEA / Eclipse에서 Spring Boot 애플리케이션 재시작

### ✅ 서버 시작 확인
```
콘솔에서 다음 메시지 확인:
"Started DemoApplication in X.XXX seconds"
```

---

## 3️⃣ 프론트엔드 앱 실행

```bash
cd front/main
flutter run
```

또는 VS Code에서 F5 (디버그 실행)

---

## 4️⃣ 기능 테스트

### 테스트 시나리오:

#### 1단계: 예약 생성 및 완료
1. 앱 실행 → 로그인
2. 병원 또는 펫시터 예약 생성
3. **관리자/파트너 권한으로 예약 상태를 `COMPLETED`로 변경**
   - 직접 데이터베이스에서 변경:
   ```sql
   UPDATE reservation
   SET status = 'COMPLETED'
   WHERE id = {예약ID};
   ```
   - 또는 파트너 앱에서 "진료 완료" 처리

#### 2단계: 상담 요청
1. 앱에서 "내 예약" 페이지로 이동
2. 완료된 예약 확인
3. **"이 예약에 대해 추가 상담하기" 버튼이 보이는지 확인** ✅
4. 버튼 클릭
5. 상담 요청 페이지로 이동되는지 확인
6. 예약 정보(파트너명, 날짜, 시간)가 자동으로 표시되는지 확인
7. 상담 제목 입력: "약 복용 관련 문의"
8. 상담 내용 입력: "처방받은 약을 먹었는데 졸려요"
9. "상담 요청하기" 버튼 클릭
10. **"상담 요청이 완료되었습니다" 메시지 표시되는지 확인** ✅

#### 3단계: 상담 상태 확인
1. "내 예약" 페이지로 돌아오기
2. 목록 새로고침
3. 해당 예약의 상담 상태 확인 (UI에 표시되는지 확인)

#### 4단계: 답변 작성 (선택사항)
파트너가 답변을 작성하려면 다음 API 호출:
```bash
curl -X PATCH "http://localhost:9075/api/v1/reservations/{예약ID}/consultation/answer?partnerId={파트너ID}&answer=그 약의 부작용이 맞습니다. 복용 시간을 저녁으로 조정해보세요."
```

또는 Postman 사용

#### 5단계: 답변 확인
1. 앱에서 "내 예약" 새로고침
2. 답변이 표시되는지 확인
3. 상담 상태가 "답변 완료"로 변경되었는지 확인

---

## 5️⃣ 체크리스트 ✅

### 데이터베이스
- [ ] `consultation_subject` 컬럼 추가됨
- [ ] `consultation_content` 컬럼 추가됨
- [ ] `consultation_answer` 컬럼 추가됨
- [ ] `consultation_status` 컬럼 추가됨 (기본값: 'NONE')
- [ ] `consultation_requested_at` 컬럼 추가됨
- [ ] `consultation_answered_at` 컬럼 추가됨
- [ ] `idx_consultation_status` 인덱스 생성됨

### 백엔드
- [ ] 서버가 정상적으로 시작됨
- [ ] 에러 로그 없음

### 프론트엔드
- [ ] 앱이 정상적으로 실행됨
- [ ] 빌드 에러 없음

### 기능 테스트
- [ ] 완료된 예약에 "추가 상담" 버튼 표시됨
- [ ] 버튼 클릭 시 상담 페이지로 이동됨
- [ ] 예약 정보가 자동으로 표시됨
- [ ] 상담 제목 입력 가능
- [ ] 상담 내용 입력 가능
- [ ] 상담 요청 성공 메시지 표시됨
- [ ] "내 예약"에서 상담 상태 확인 가능
- [ ] (선택) 파트너 답변 확인 가능

---

## 🐛 문제 발생 시 확인 사항

### 1. 백엔드 서버가 시작되지 않는 경우
```
오류: "Unknown column 'consultation_status'"
→ 데이터베이스 마이그레이션이 실행되지 않았습니다. 1단계로 돌아가서 SQL 실행
```

### 2. 프론트엔드 빌드 오류
```bash
# 의존성 업데이트
cd front/main
flutter pub get
flutter clean
flutter pub get
flutter run
```

### 3. API 호출 실패
```
오류: 404 Not Found
→ 백엔드 서버가 실행 중인지 확인 (http://localhost:9075)

오류: 500 Internal Server Error
→ 백엔드 콘솔 로그 확인
→ 데이터베이스 컬럼이 제대로 추가되었는지 확인
```

### 4. "추가 상담" 버튼이 안 보이는 경우
```
→ 예약 상태가 'COMPLETED'인지 확인
→ my_reservation.dart 코드에 버튼이 제대로 추가되었는지 확인
```

---

## 📞 도움말

문제가 발생하면:
1. 백엔드 콘솔 로그 확인
2. 프론트엔드 콘솔 로그 확인 (flutter run 출력)
3. 데이터베이스에서 직접 확인:
   ```sql
   -- 컬럼 확인
   SHOW COLUMNS FROM reservation;

   -- 테스트 데이터 확인
   SELECT id, status, consultation_status, consultation_subject
   FROM reservation
   WHERE status = 'COMPLETED';
   ```

---

**중요:** 1단계(데이터베이스 마이그레이션)를 먼저 실행하지 않으면 백엔드가 시작되지 않습니다! ⚠️

**작업 순서:** 1 → 2 → 3 → 4 (순서대로 진행하세요!)
