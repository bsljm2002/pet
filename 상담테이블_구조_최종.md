# 상담(Consultations) 테이블 구조 - 최종 버전

## 📋 개요
파트너(수의사/펫시터)와 고객 간의 상담 기능을 지원하는 테이블입니다.
- 예약 연결 상담과 독립 상담(간편 상담) 모두 지원
- 이미지 첨부 가능
- 상담 상태를 ENUM으로 관리하여 데이터 무결성 보장

---

## 🗂️ 테이블 생성 SQL

```sql
CREATE TABLE IF NOT EXISTS consultations (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '상담 ID',
    user_id BIGINT NOT NULL COMMENT '고객 ID (users 테이블 FK)',
    partner_id BIGINT NOT NULL COMMENT '파트너 ID (partners 테이블 FK)',
    reservation_id BIGINT NULL COMMENT '연결된 예약 ID (선택적, reservations 테이블 FK)',
    pet_type VARCHAR(20) NOT NULL COMMENT '반려동물 종류 (강아지, 고양이 등)',
    subject VARCHAR(100) NOT NULL COMMENT '상담 제목',
    content TEXT NOT NULL COMMENT '상담 내용',
    image_url VARCHAR(500) NULL COMMENT '첨부 이미지 URL (선택적)',
    answer TEXT NULL COMMENT '파트너 답변',
    answered_at DATETIME NULL COMMENT '답변 일시',
    status ENUM('PENDING', 'ANSWERED', 'CLOSED', 'CANCELLED')
        NOT NULL DEFAULT 'PENDING'
        COMMENT '상담 상태 (PENDING: 대기중, ANSWERED: 답변완료, CLOSED: 종료, CANCELLED: 취소)',
    created_at DATETIME NOT NULL COMMENT '생성 일시',

    -- 외래키 제약 조건
    FOREIGN KEY (user_id) REFERENCES users(id)
        ON DELETE CASCADE
        COMMENT '사용자 삭제 시 상담도 삭제',

    FOREIGN KEY (partner_id) REFERENCES partners(id)
        ON DELETE CASCADE
        COMMENT '파트너 삭제 시 상담도 삭제',

    FOREIGN KEY (reservation_id) REFERENCES reservations(id)
        ON DELETE SET NULL
        COMMENT '예약 삭제 시 상담은 유지하고 연결만 해제',

    -- 인덱스
    INDEX idx_user_id (user_id),
    INDEX idx_partner_id (partner_id),
    INDEX idx_reservation_id (reservation_id),
    INDEX idx_status (status),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 📊 컬럼 상세 정보

| 컬럼명 | 타입 | NULL | 기본값 | 설명 |
|--------|------|------|--------|------|
| `id` | BIGINT | NO | AUTO_INCREMENT | 상담 고유 ID (Primary Key) |
| `user_id` | BIGINT | NO | - | 고객 ID (users 테이블 외래키) |
| `partner_id` | BIGINT | NO | - | 파트너 ID (partners 테이블 외래키) |
| `reservation_id` | BIGINT | YES | NULL | 예약 ID (reservations 테이블 외래키, 선택적) |
| `pet_type` | VARCHAR(20) | NO | - | 반려동물 종류 (예: "강아지", "고양이") |
| `subject` | VARCHAR(100) | NO | - | 상담 제목 |
| `content` | TEXT | NO | - | 상담 내용 |
| `image_url` | VARCHAR(500) | YES | NULL | 첨부 이미지 URL (선택적) |
| `answer` | TEXT | YES | NULL | 파트너의 답변 내용 |
| `answered_at` | DATETIME | YES | NULL | 답변 완료 일시 |
| `status` | ENUM | NO | 'PENDING' | 상담 상태 |
| `created_at` | DATETIME | NO | - | 상담 생성 일시 |

---

## 🔑 외래키 관계 및 동작

### 1. user_id → users(id)
- **동작**: `ON DELETE CASCADE`
- **의미**: 사용자가 탈퇴하면 해당 사용자의 모든 상담 기록 삭제
- **이유**: 개인정보 보호 및 데이터 일관성 유지

### 2. partner_id → partners(id)
- **동작**: `ON DELETE CASCADE`
- **의미**: 파트너가 삭제되면 해당 파트너의 모든 상담 기록 삭제
- **이유**: 파트너 정보 없이 상담 기록은 의미 없음

### 3. reservation_id → reservations(id)
- **동작**: `ON DELETE SET NULL`
- **의미**: 예약이 삭제되어도 상담 기록은 유지, 단 예약 연결만 해제
- **이유**: 상담 내역은 독립적으로 보존할 가치가 있음
- **특징**: NULL 허용으로 예약 없는 간편 상담 지원

---

## 📌 상담 상태 (status) ENUM 값

| 값 | 한글명 | 설명 | 사용 시점 |
|----|--------|------|-----------|
| `PENDING` | 대기중 | 상담 요청 후 답변 대기 상태 | 상담 생성 시 기본값 |
| `ANSWERED` | 답변완료 | 파트너가 답변을 완료한 상태 | 파트너 답변 작성 시 |
| `CLOSED` | 종료 | 상담이 완전히 종료된 상태 | 고객 또는 파트너가 종료 시 |
| `CANCELLED` | 취소 | 상담이 취소된 상태 | 고객이 상담 취소 시 |

### ENUM 사용의 장점:
✅ **오타 방지**: 정의된 값만 허용, 잘못된 값 입력 시 DB 에러 발생
✅ **대소문자 통일**: 자동으로 통일된 형식 유지
✅ **성능 향상**: VARCHAR보다 저장 공간 효율적 (1-2바이트)
✅ **데이터 무결성**: 허용된 상태 값만 저장 보장
✅ **가독성**: 코드에서 명확한 상태 표현

---

## 🔍 인덱스 설명

| 인덱스명 | 컬럼 | 목적 |
|----------|------|------|
| `idx_user_id` | user_id | 특정 사용자의 상담 목록 조회 속도 향상 |
| `idx_partner_id` | partner_id | 특정 파트너의 상담 목록 조회 속도 향상 |
| `idx_reservation_id` | reservation_id | 예약 연결 상담 조회 속도 향상 |
| `idx_status` | status | 상태별 상담 필터링 속도 향상 |
| `idx_created_at` | created_at | 날짜순 정렬 조회 속도 향상 |

---

## 📝 사용 예시

### 1. 예약 없는 간편 상담 생성
```sql
INSERT INTO consultations (
    user_id,
    partner_id,
    reservation_id,  -- NULL
    pet_type,
    subject,
    content,
    status,
    created_at
) VALUES (
    1,                -- 고객 ID
    5,                -- 파트너 ID
    NULL,             -- 예약 없음
    '강아지',         -- 반려동물 종류
    '일반 질문',      -- 제목
    '강아지가 초콜릿을 먹으면 안 되나요?',
    'PENDING',        -- 대기중
    NOW()
);
```

### 2. 예약 연결 상담 생성
```sql
INSERT INTO consultations (
    user_id,
    partner_id,
    reservation_id,  -- 예약 ID
    pet_type,
    subject,
    content,
    image_url,       -- 이미지 첨부
    status,
    created_at
) VALUES (
    1,
    5,
    123,              -- 예약 ID
    '고양이',
    '예약 관련 문의',
    '예약 시간 변경이 가능한가요?',
    'https://example.com/images/cat.jpg',
    'PENDING',
    NOW()
);
```

### 3. 파트너 답변 작성
```sql
UPDATE consultations
SET
    answer = '네, 예약 시간 변경이 가능합니다. 원하시는 시간을 알려주세요.',
    answered_at = NOW(),
    status = 'ANSWERED'
WHERE id = 1;
```

### 4. 상담 상태 변경
```sql
-- 상담 종료
UPDATE consultations
SET status = 'CLOSED'
WHERE id = 1;

-- 상담 취소
UPDATE consultations
SET status = 'CANCELLED'
WHERE id = 1;
```

---

## 🔎 조회 쿼리 예시

### 1. 특정 사용자의 상담 목록 조회
```sql
SELECT
    c.id,
    c.subject,
    c.pet_type,
    c.status,
    c.created_at,
    p.name AS partner_name,
    r.reservation_date
FROM consultations c
JOIN partners p ON c.partner_id = p.id
LEFT JOIN reservations r ON c.reservation_id = r.id
WHERE c.user_id = 1
ORDER BY c.created_at DESC;
```

### 2. 특정 파트너의 대기중인 상담 목록
```sql
SELECT
    c.id,
    c.subject,
    c.content,
    c.pet_type,
    c.created_at,
    u.nickname AS user_name
FROM consultations c
JOIN users u ON c.user_id = u.id
WHERE c.partner_id = 5
  AND c.status = 'PENDING'
ORDER BY c.created_at ASC;
```

### 3. 특정 예약과 연결된 상담 조회
```sql
SELECT *
FROM consultations
WHERE reservation_id = 123;
```

### 4. 답변 완료된 상담 통계
```sql
SELECT
    partner_id,
    COUNT(*) AS total_answered,
    AVG(TIMESTAMPDIFF(HOUR, created_at, answered_at)) AS avg_response_hours
FROM consultations
WHERE status = 'ANSWERED'
GROUP BY partner_id;
```

---

## ⚠️ 주의사항 및 제약사항

### 1. 데이터 무결성
- ✅ **ENUM으로 상태 통일**: 'PENDING', 'ANSWERED', 'CLOSED', 'CANCELLED'만 허용
- ✅ **외래키로 참조 무결성**: 존재하지 않는 user_id, partner_id, reservation_id 입력 불가
- ✅ **NULL 허용 설계**: reservation_id는 NULL 가능 (독립 상담 지원)

### 2. 삭제 동작
```sql
-- 예약 삭제 시
DELETE FROM reservations WHERE id = 123;
-- 결과: consultations.reservation_id = NULL로 변경 (상담 유지)

-- 사용자 삭제 시
DELETE FROM users WHERE id = 1;
-- 결과: 해당 사용자의 모든 상담 삭제 (CASCADE)

-- 파트너 삭제 시
DELETE FROM partners WHERE id = 5;
-- 결과: 해당 파트너의 모든 상담 삭제 (CASCADE)
```

### 3. 필수 입력값
- `user_id`, `partner_id`, `pet_type`, `subject`, `content`는 필수
- `reservation_id`, `image_url`, `answer`, `answered_at`는 선택적

### 4. 상태 변경 규칙
```sql
-- 정상적인 상태 전이
PENDING → ANSWERED   -- 파트너 답변 작성
PENDING → CANCELLED  -- 고객 취소
ANSWERED → CLOSED    -- 상담 종료

-- 비정상적인 상태 전이 (로직에서 방지 필요)
ANSWERED → PENDING   -- 불가
CLOSED → PENDING     -- 불가
CANCELLED → ANSWERED -- 불가
```

---

## 🔧 백엔드 연동 (Java/Spring Boot)

### Consultation 엔티티
```java
package com.example.pet.demo.consultation.domain;

import com.example.pet.demo.partner.domain.Partner;
import com.example.pet.demo.reservation.domain.Reservation;
import com.example.pet.demo.users.domain.User;
import jakarta.persistence.*;
import lombok.*;

import java.time.LocalDateTime;

@Entity
@Table(name = "consultations")
@Getter
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class Consultation {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "user_id", nullable = false)
    private User user;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "partner_id", nullable = false)
    private Partner partner;

    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "reservation_id", nullable = true)  // NULL 허용
    private Reservation reservation;

    @Column(name = "pet_type", nullable = false, length = 20)
    private String petType;

    @Column(nullable = false, length = 100)
    private String subject;

    @Column(nullable = false, columnDefinition = "TEXT")
    private String content;

    @Column(name = "image_url", length = 500)
    private String imageUrl;

    @Column(columnDefinition = "TEXT")
    private String answer;

    @Column(name = "answered_at")
    private LocalDateTime answeredAt;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private ConsultationStatus status = ConsultationStatus.PENDING;

    @Column(name = "created_at", nullable = false, updatable = false)
    private LocalDateTime createdAt;

    @PrePersist
    protected void onCreate() {
        this.createdAt = LocalDateTime.now();
    }

    /**
     * 답변 작성
     */
    public void addAnswer(String answer) {
        this.answer = answer;
        this.answeredAt = LocalDateTime.now();
        this.status = ConsultationStatus.ANSWERED;
    }

    /**
     * 상담 종료
     */
    public void close() {
        this.status = ConsultationStatus.CLOSED;
    }

    /**
     * 상담 취소
     */
    public void cancel() {
        this.status = ConsultationStatus.CANCELLED;
    }
}
```

### ConsultationStatus ENUM
```java
package com.example.pet.demo.consultation.domain;

public enum ConsultationStatus {
    PENDING("대기중"),
    ANSWERED("답변완료"),
    CLOSED("종료"),
    CANCELLED("취소");

    private final String description;

    ConsultationStatus(String description) {
        this.description = description;
    }

    public String getDescription() {
        return description;
    }
}
```

---

## 📈 성능 최적화 팁

### 1. 인덱스 활용
```sql
-- 복합 인덱스 추가 고려 (상황에 따라)
CREATE INDEX idx_partner_status ON consultations(partner_id, status);
CREATE INDEX idx_user_status ON consultations(user_id, status);
```

### 2. 쿼리 최적화
```java
// N+1 문제 방지: Fetch Join 사용
@Query("SELECT c FROM Consultation c " +
       "JOIN FETCH c.user " +
       "JOIN FETCH c.partner " +
       "LEFT JOIN FETCH c.reservation " +
       "WHERE c.partner.id = :partnerId")
List<Consultation> findByPartnerIdWithDetails(@Param("partnerId") Long partnerId);
```

### 3. 페이징 처리
```java
// 대량 데이터 조회 시 페이징 필수
Page<Consultation> findByUserId(Long userId, Pageable pageable);
```

---

## 📚 관련 테이블

- **users**: 고객 정보
- **partners**: 파트너(수의사/펫시터) 정보
- **reservations**: 예약 정보 (선택적 연결)

---

## 🎯 체크리스트

데이터베이스 생성 시 확인사항:

- [ ] 외래키 제약조건 적용 확인
- [ ] ENUM 타입 값 정확히 설정
- [ ] 인덱스 생성 확인
- [ ] 기본값(DEFAULT) 설정 확인
- [ ] 문자셋(utf8mb4) 설정 확인
- [ ] 엔진(InnoDB) 설정 확인
- [ ] ON DELETE 동작 테스트

---

**문서 버전**: 1.0 (최종)
**작성일**: 2025-11-26
**주요 변경사항**:
- ENUM 타입으로 status 관리
- NULL 허용 외래키로 reservation_id 설정
- 데이터 무결성 강화
